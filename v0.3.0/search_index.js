var documenterSearchIndex = {"docs":
[{"category":"section","location":"man/storyboard/#storyboard-page","page":"Storyboard","text":"storyboard stitches an ordered sequence of frames into a single static figure — the filmstrip you put on a poster or in a paper.","title":"Storyboard"},{"category":"section","location":"man/storyboard/#A-single-row,-in-reading-order","page":"Storyboard","text":"tierbrief: Tierbrief\nPass one panel per frame, oldest first. Panels fill left to right, and each panel's title is where its timestamp goes.\n\ntierfull: Tierfull\nOrder is the whole difference from montage. A montage compares things that have no inherent sequence, so it packs them into a near-square grid; a storyboard is one subject over time, so it defaults to a single row (ncols = length(panels)) and reading order is time order. Titles are exposed for exactly this reason: a frame that does not say when it was taken leaves the reader counting panels.Cells are uniform and sized from the largest aspect ratio among the panels, so frames that differ slightly in intrinsic size still line up rather than clip. The title band is reserved once for the whole strip, and only if at least one panel carries a title.\n\nsvg = storyboard([Panel(frames[i]; title = \"t = $(times[i])\") for i in 1:4]; output = nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"1260.0\\\" height=\\\"358.0\\\" viewBox=\\\"0 0 1260.0 358.0\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"1260.0\\\" height=\\\"358.0\\\" fill=\\\"white\\\"/>\\n<text x=\\\"162.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 0</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"15\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 120</text>\\n<svg x=\\\"324.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"22\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"786.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 240</text>\\n<svg x=\\\"636.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"29\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"1098.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 360</text>\\n<svg x=\\\"948.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"36\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n</svg>\\n\")","title":"A single row, in reading order"},{"category":"section","location":"man/storyboard/#Wrapping-a-long-strip","page":"Storyboard","text":"tierbrief: Tierbrief\nncols sets the number of columns. Panels keep the order you gave them and fill row-major, so time runs left to right and then down.\n\nsvg = storyboard([Panel(frames[i]; title = \"t = $(times[i])\") for i in eachindex(frames)];\n                 ncols = 3, output = nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"948.0\\\" height=\\\"704.0\\\" viewBox=\\\"0 0 948.0 704.0\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"948.0\\\" height=\\\"704.0\\\" fill=\\\"white\\\"/>\\n<text x=\\\"162.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 0</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"15\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 120</text>\\n<svg x=\\\"324.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"22\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"786.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 240</text>\\n<svg x=\\\"636.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"29\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"162.0\\\" y=\\\"382.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 360</text>\\n<svg x=\\\"12.0\\\" y=\\\"392.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"36\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"382.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 480</text>\\n<svg x=\\\"324.0\\\" y=\\\"392.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"43\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"786.0\\\" y=\\\"382.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 600</text>\\n<svg x=\\\"636.0\\\" y=\\\"392.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n</svg>\\n\")","title":"Wrapping a long strip"},{"category":"section","location":"man/storyboard/#Static-by-design","page":"Storyboard","text":"tierbrief: Tierbrief\nA panel whose content is a vector of frame paths is rejected. To animate one subject's frames, hand them to montage as a single animated panel instead — see Movies.\n\ntierfull: Tierfull\nA filmstrip and a movie are different artefacts, not two renderings of one thing. The strip shows every timepoint at once, to be scanned and compared and printed; the movie shows one timepoint at a time, to be played. Which of the two you want is a decision about the figure, and a verb that quietly produced either one depending on the shape of its argument would make that decision invisible at the call site — you would have to inspect the panels to know what you were about to get. So storyboard commits to the strip and says so loudly.\n\nstoryboard([Panel(frames)])\n# ERROR: storyboard is static — each panel must be a single image, not a frame\n# sequence; use `montage` for movies\n\nmontage([Panel(frames; title = \"colony\")]; output = \"colony.mp4\")   # the movie of those frames","title":"Static by design"},{"category":"section","location":"man/storyboard/#Writing-the-figure","page":"Storyboard","text":"tierbrief: Tierbrief\nstoryboard writes storyboard.svg in the current directory and also returns the composed SVG as a String. Point output somewhere else, set it to nothing to skip writing entirely, and pass overwrite = true to replace a file that already exists — without it, an existing output is an error rather than a silent clobber.\n\nstoryboard(panels)                              # → ./storyboard.svg, and returns the String\nstoryboard(panels; output = \"figs/fig2.svg\")    # any other path (directories are created)\nstoryboard(panels; output = nothing)            # the String only, nothing written\nstoryboard(panels; overwrite = true)            # replace an existing file","title":"Writing the figure"},{"category":"section","location":"man/storyboard/#Legends","page":"Storyboard","text":"tierbrief: Tierbrief\nlegend says what to draw and legend_position says where, exactly as in Montage. The one difference is placement: a single-row strip has no spare grid cell, so legend_position = :auto falls back to a full-width band under the frames. Wrap the strip with ncols and any trailing free cells become available again.\n\nsvg = storyboard([Panel(frames[i]; title = \"t = $(times[i])\") for i in 1:4];\n                 legend = [(\"colony\", \"seagreen\")], output = nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"1260.0\\\" height=\\\"407.4\\\" viewBox=\\\"0 0 1260.0 407.4\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"1260.0\\\" height=\\\"407.4\\\" fill=\\\"white\\\"/>\\n<text x=\\\"162.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 0</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"15\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 120</text>\\n<svg x=\\\"324.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"22\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"786.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 240</text>\\n<svg x=\\\"636.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"29\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"1098.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">t = 360</text>\\n<svg x=\\\"948.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"36\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<circle cx=\\\"587.32\\\" cy=\\\"376.7\\\" r=\\\"9.9\\\" stroke=\\\"black\\\" stroke-width=\\\"1\\\" fill=\\\"seagreen\\\"/>\\n<text x=\\\"606.02\\\" y=\\\"384.4\\\" font-family=\\\"Arial\\\" font-size=\\\"22.0\\\" fill=\\\"black\\\">colony</text>\\n\\n</svg>\\n\")","title":"Legends"},{"category":"section","location":"man/storyboard/#PhysiCell-runs","page":"Storyboard","text":"tierbrief: Tierbrief\nA storyboard can be built straight from a simulation, which picks the snapshots, titles each frame with its time, and takes the cell-type legend from the run itself. Those keywords are documented on PhysiCell simulations.\n\nusing PhysiCellModelManager, Montage\n\nstoryboard(Simulation, 1)","title":"PhysiCell runs"},{"category":"section","location":"man/tableau/#tableau-page","page":"Tableau","text":"tableau composes one state as a focal panel surrounded by satellite panels — the verb for showing how the heterogeneous parts of a single state relate spatially.\n\ntierbrief: Tierbrief\nWhere montage and storyboard stitch pictures that already exist, tableau re-plots from data. That is what buys real heatmaps, real colorbars and one spatial extent shared by every panel, and it is why the verb needs CairoMakie.\n\ntierfull: Tierfull\nThe methods appear as soon as CairoMakie is loaded alongside Montage; until then, calling tableau errors with exactly that hint. CairoMakie is a weak dependency, so its load time falls only on the people who ask for this verb — see Extensions.\n\nusing CairoMakie, Montage\n\ntierjournal: 2026-07-22 — tableau is CairoMakie-only\nThe other two verbs stitch existing SVGs, which cannot give real heatmaps, real colorbars or a shared spatial extent. Rejected forcing tableau into the SVG backend to keep it in the core; it would have produced a picture that looked like a tableau without the axes actually agreeing. CairoMakie stays a weak dependency, so the cost lands only on people who ask for this verb.","title":"Tableau"},{"category":"section","location":"man/tableau/#The-generic-form","page":"Tableau","text":"tierbrief: Tierbrief\ntableau(focal, satellites) takes axis callbacks, not data: focal is ax -> …, and each satellite is ax -> plot. Every satellite callback must return the plot it drew — that returned object is what its colorbar reads.\n\ntierfull: Tierfull\nCallbacks rather than arrays, because the verb owns the layout and nothing else. A focal panel is whatever you can draw into a Makie Axis — a scatter, a contour, a rasterized image — and a satellite is any plot a Colorbar accepts, so there is no data shape for the generic method to know about. The methods that do take data, for PhysiCell output, are built on top of this one by writing the callbacks for you.satellite_titles and colorbar_labels run parallel to satellites: entry i labels panel i, and a short vector simply leaves the later panels unlabelled. xlims and ylims are given once and applied to every axis, focal and satellite alike, so a feature at a given coordinate sits at the same place in all of them — the whole point of the arrangement. Both must be given for either to take effect. size is the figure size in Makie's device-independent pixels, (1000, 1000) by default.\n\nfunction cells(ax)\n    scatter!(ax, tumor; label = \"tumor\", markersize = 7)\n    scatter!(ax, immune; label = \"immune\", markersize = 7)\nend\n\ntableau(cells,\n        [ax -> heatmap!(ax, xs, ys, oxygen),\n         ax -> heatmap!(ax, xs, ys, drug),\n         ax -> heatmap!(ax, xs, ys, ecm)];\n        focal_title = \"cells\",\n        satellite_titles = [\"oxygen\", \"drug\", \"ECM\"],\n        colorbar_labels = [\"mmHg\", \"μM\", \"density\"],\n        xlims = (-100, 100), ylims = (-100, 100),\n        size = (800, 800),\n        output = nothing)\nFigure()","title":"The generic form"},{"category":"section","location":"man/tableau/#Where-things-land","page":"Tableau","text":"tierbrief: Tierbrief\nSatellites ring the centred focal panel in a fixed order: north, south, west, east, then the four corners. Past eight satellites the ring gives way to a square grid with the focal panel in the middle of it.\n\ntierfull: Tierfull\nSatellite axes have their decorations hidden. They share the focal panel's extent, so their ticks would restate the focal panel's three to eight times over; the focal axis keeps its ticks and carries the coordinates for the whole figure.The legend is built from the labelled plots on the focal axis, so give each focal series a label and leave the satellites alone — their key is the colorbar beside them.\n\nlegend where the legend goes\n:auto (default) the empty grid cell nearest the focal panel, costing no space; an in-axis corner when the grid is full\na Symbol (:rt, :lb, …) that corner, inside the focal axis, on an opaque background\n(row, col) that grid cell — warns if it collides with the focal panel or a satellite\nnothing no legend","title":"Where things land"},{"category":"section","location":"man/tableau/#A-colorbar-on-the-focal-panel","page":"Tableau","text":"tierbrief: Tierbrief\nfocal_colorbar_label gives the focal panel a colorbar of its own, for a focal plot whose colour encodes a continuous value rather than discrete categories.\n\ntierfull: Tierfull\nThe focal panel then moves into the same axis-plus-colorbar cell the satellites use, and the focal callback must return its plot — the convention the satellites already follow. Such a plot carries no labelled series for a legend to read, so pass legend=nothing alongside it.\n\ntableau(ax -> scatter!(ax, positions; color = pressure, colormap = :plasma),\n        [ax -> heatmap!(ax, xs, ys, oxygen)];\n        focal_colorbar_label = \"pressure\", legend = nothing,\n        satellite_titles = [\"oxygen\"], output = \"scene.pdf\")","title":"A colorbar on the focal panel"},{"category":"section","location":"man/tableau/#Output-and-file-formats","page":"Tableau","text":"tierbrief: Tierbrief\ntableau writes tableau.png and returns that path; output=nothing returns the Makie Figure instead. Writing errors if the file already exists unless you pass overwrite=true.\n\ntierfull: Tierfull\nThe file extension on output picks the format — CairoMakie renders .png, .pdf and .svg, so a publication-ready vector figure is just a different filename. One cell layer (511 cells) plus a single 50×50 voxel substrate comes out very differently in the three:\n\nformat size notes\n.png 154 KB raster; the default, best for exploring\n.pdf 52 KB vector, stream-compressed, and the smallest of the three — the publication choice\n.svg 739 KB vector, but uncompressed text, so much the largest\n\ntierfull: Tierfull\nTwo things are worth knowing before reaching for .svg. CairoMakie's SVG contains no <text> elements at all — every label is emitted as glyph outlines, so an axis label cannot be retyped in Illustrator. (montage and storyboard stitch rather than render, and do emit real <text>, so their titles and legends stay editable; that is the path to take when editable text is the requirement.) And a heatmap becomes one path per voxel: roughly 2500 of them for a 50×50 grid, which is where the bulk of that 739 KB is, so a finer mesh inflates the file faster than more cells do.A tableau movie needs a video container, and asking for .svg there fails in Makie's recorder rather than quietly producing something odd.\n\ntierjournal: 2026-08-05 — PDF, not SVG, for a publication tableau\nMeasured one cell layer plus a 50x50 substrate at 154 KB (.png), 52 KB (.pdf) and 739 KB (.svg). PDF is vector AND stream-compressed, so it is the smallest of the three — the opposite of the \"vector costs you size\" caveat that was going to be written. The SVG bulk is ~2500 heatmap paths, not the cell scatter, so a finer mesh inflates it faster than more cells do.","title":"Output and file formats"},{"category":"section","location":"man/tableau/#From-a-simulation","page":"Tableau","text":"tierbrief: Tierbrief\nLoading PhysiCellOutput or PhysiCellModelManager adds methods that build the callbacks for you from a snapshot's cells and substrates, along with keywords for choosing which cells appear and what their colour means — substrates, color, color_mode, cell_colormap, cell_types and include_dead. Those are documented in PhysiCell simulations.\n\nusing CairoMakie, PhysiCellModelManager, Montage\n\ntableau(Simulation, 1; output = \"figure.pdf\")","title":"From a simulation"},{"category":"section","location":"man/tableau/#Animating","page":"Tableau","text":"tierbrief: Tierbrief\nA tableau is a movie when its index covers several timepoints: the layout above is built once and the whole scene is stepped through Makie.record, with the plotting callbacks drawing Observables that the record loop updates per frame. You reach it through a data source rather than through the generic method — see Movies.\n\ntableau(Simulation, 1; index = :all, output = \"scene.mp4\")","title":"Animating"},{"category":"section","location":"reference/#reference-page","page":"API reference","text":"Every exported name, plus every method the package extensions add once their packages are loaded. Internal helpers are documented under Architecture.\n\nPages = [\"reference.md\"]","title":"API reference"},{"category":"section","location":"reference/#Composition-types","page":"API reference","text":"","title":"Composition types"},{"category":"section","location":"reference/#The-verbs","page":"API reference","text":"","title":"The verbs"},{"category":"section","location":"reference/#PhysiCell-output-folders","page":"API reference","text":"Available with using PhysiCellOutput (or using PhysiCellModelManager, which depends on it). See PhysiCell simulations.","title":"PhysiCell output folders"},{"category":"section","location":"reference/#PhysiCell-simulations-by-id","page":"API reference","text":"Available with using PhysiCellModelManager.","title":"PhysiCell simulations by id"},{"category":"section","location":"reference/#Tableau","page":"API reference","text":"Available with using CairoMakie; the simulation methods also need a PhysiCell package.","title":"Tableau"},{"category":"type","location":"reference/#Montage.MontageSpec","page":"API reference","text":"MontageSpec\n\nA movie-able composition: a uniform grid of frame-sequence panels (each panel's content is a Vector of per-timepoint frame paths), a common nframes count, and the grid layout parameters. Built by montage when its panels are animated, and consumed by record.\n\nFrames are aligned by index and truncated to the shortest sequence (see nframes).\n\nThe legend_svg/legend_position/legend_font_size fields carry the composition's legend (see _svgGrid) so every rendered frame draws the same one — a legend that would otherwise flicker or vanish mid-movie. The five-argument constructor omits them (no legend).\n\n\n\n\n\n","title":"Montage.MontageSpec"},{"category":"type","location":"reference/#Montage.Panel","page":"API reference","text":"Panel(content; title=\"\", transform=identity)\nPanel(content, title)\n\nOne cell of a composition: some content plus an optional title drawn above it.\n\nArguments\n\ncontent: what to draw. For the SVG backend this is a path to an SVG file (AbstractString). Future Makie-backed content (a plotting callback + data) also lands here.\ntitle::AbstractString: text drawn in a band above the content. An empty title (the default) means no title band is reserved for this panel — an all-untitled composition has no wasted vertical space.\ntransform: a function applied to this panel's SVG text before it is placed — SVG text -> SVG text. The seam for editing a panel's contents without re-rendering it: the PhysiCell extension uses it to drop or recolour cells. identity (the default) is free — it returns the very same object, so the output is byte-identical to no transform at all.\nA movie panel's content is a list of frames, and transform can match it one-for-one:\none function — applied to every frame (e.g. \"drop the nk cells\", which does not depend on time);\na list of functions, the same length as the frames — then frame t is edited by transform[t].\nThe per-frame form exists because some edits do depend on the timepoint: colouring cells by a data value needs each frame mapped from that snapshot's own values, so each frame needs its own function.\n\nExamples\n\nPanel(\"output/final.svg\"; title=\"Sim 1\")\nPanel(\"output/final.svg\")                                  # untitled\nPanel(\"output/final.svg\"; transform = s -> replace(s, \"red\" => \"blue\"))\n\n\n\n\n\n","title":"Montage.Panel"},{"category":"method","location":"reference/#Montage.montage-Tuple{Any}","page":"API reference","text":"montage(panels; backend=:svg, ncols=<auto: ceil(sqrt(n))>, panel_width=300,\n        title_height=34, pad=12,\n        legend=nothing, legend_position=:auto, legend_font_size=<title size>,\n        output=<auto: montage.svg | montage.mp4>, overwrite=false, framerate=15)\n\nCompose panels into a uniform titled grid — the verb for comparing like-for-like across many things (e.g. the final state of every simulation in a batch, side by side).\n\nThe panel content decides still image vs. movie, in one call: if every panel's content is a single image (an SVG path), the result is a still image; if any panel's content is a Vector of frame paths (one per timepoint), every panel plays its frames in lockstep and the result is a movie (rendered via record, which needs using Rsvg, Cairo, FFMPEG).\n\nArguments\n\npanels: a Vector{Panel}, or a loose vector of raw contents (each wrapped as an untitled Panel). A panel's content is an SVG path (still) or a Vector of SVG paths (frames of a movie).\n\nKeyword Arguments\n\nbackend::Symbol=:svg: :svg (default, built into the core — lossless vector, static) or :makie (real heatmaps/colorbars; requires using CairoMakie).\npanel_width::Real=300: displayed width in px of each panel.\ntitle_height::Real=34: px reserved above each panel for its title. The band is reserved for the whole grid only if at least one panel is titled.\npad::Real=12: px of padding between and around panels.\nncols::Union{Nothing,Integer}=nothing: columns in the grid; nothing (the default) keeps the near-square ceil(sqrt(n)) layout. Set it to shape the comparison — e.g. a conditions-by-replicates grid via ncols=<number of conditions> with panels listed row-major. Applies to stills and movies alike.\nlegend: what legend to draw — [(\"label\", \"color\"), …] entries (drawn as flat circles and labels, so they stay editable in Illustrator/PowerPoint), or a path/SVG string to nest as-is. nothing (the core default) draws none. With PhysiCellOutput or PCMM loaded the default becomes :auto, which means \"take the cell types from the run's own legend.svg\".\nlegend_position: where it goes — :auto (the free cells trailing the last row if the layout has any, so the figure does not grow, else a full-width band below), :bottom, :top, or an explicit (row, col) / (row, col, span) cell. Independent of legend, so any content can take any placement.\nlegend_font_size::Real: text size for a drawn legend. Defaults to the panel-title size, so the two match. Drawn legends keep this size and wrap to fit the space; a nested SVG legend instead sits at its natural size, shrunk only if it will not fit.\noutput::Union{Nothing,AbstractString}: where to write the result, in the current directory by default — montage.svg for a still image, montage.mp4 for a movie. Errors if the file exists unless overwrite=true. Pass output=nothing to skip writing and return the in-memory result instead.\noverwrite::Bool=false: allow writing over an existing output file.\nframerate::Integer=15: frames per second (movies only).\n\nReturns\n\nThe written path's result: the composed SVG String for a still image, or the output path for a movie. With output=nothing, the in-memory object instead — the SVG String for a still image, or a MontageSpec for a movie (which you can then hand to record for finer control, e.g. scale).\n\nExamples\n\nusing Montage\n\n# Still image — written to ./montage.svg by default, and returned as a string\nsvg = montage([Panel(\"a/final.svg\"; title=\"A\"), Panel(\"b/final.svg\"; title=\"B\")])\nmontage([\"a/final.svg\", \"b/final.svg\"]; output=\"grid.svg\")   # or choose a path\n\n# A legend of your own, banded below rather than tucked into a spare cell\nmontage([\"a/final.svg\", \"b/final.svg\"];\n        legend=[(\"tumor\", \"grey\"), (\"immune\", \"green\")], legend_position=:bottom)\n\n# Montage of movies: each panel is a frame sequence → one call writes ./montage.mp4\nusing Rsvg, Cairo, FFMPEG                                    # movie extension\nmontage([Panel([\"a/f1.svg\", \"a/f2.svg\"]; title=\"A\"),\n         Panel([\"b/f1.svg\", \"b/f2.svg\"]; title=\"B\")]; output=\"compare.mp4\", framerate=15)\n\n\n\n\n\n","title":"Montage.montage"},{"category":"function","location":"reference/#Montage.record","page":"API reference","text":"record(spec::MontageSpec, path=\"montage.mp4\"; framerate=15, scale=1, overwrite=false) -> path\n\nRender a movie of spec to path (extension picks the container, e.g. .mp4/.gif; defaults to montage.mp4 in the current directory), with every panel playing through its frames in lockstep. Each output frame is the montage of that timepoint's frames. Errors if path exists unless overwrite=true.\n\nRequires the movie extension — run using Rsvg, Cairo, FFMPEG — otherwise a helpful error is thrown.\n\nKeyword Arguments\n\nframerate::Integer=15: frames per second.\nscale::Real=1: rasterization scale factor; >1 renders sharper (larger) frames.\noverwrite::Bool=false: allow writing over an existing path.\n\nExamples\n\nusing Montage, Rsvg, Cairo, FFMPEG\nspec = montage([Panel([\"a/f1.svg\",\"a/f2.svg\"]; title=\"A\"),\n                Panel([\"b/f1.svg\",\"b/f2.svg\"]; title=\"B\")])\nrecord(spec, \"compare.mp4\"; framerate=15)\n\n\n\n\n\n","title":"Montage.record"},{"category":"method","location":"reference/#Montage.storyboard-Tuple{Any}","page":"API reference","text":"storyboard(panels; backend=:svg, ncols=length(panels), panel_width=300,\n           title_height=34, pad=12, legend=nothing, legend_position=:auto,\n           legend_font_size=<title size>, output=\"storyboard.svg\", overwrite=false)\n\nStitch an ordered sequence of panels into a single static figure — the verb for showing one subject evolving over time (e.g. a simulation's snapshots left to right).\n\nUnlike montage (an unordered ceil(sqrt(n)) grid), storyboard lays panels out in reading order and defaults to a single row (ncols = length(panels)); set ncols to wrap the strip into that many columns while preserving time order.\n\nArguments\n\npanels: a Vector{Panel}, or a loose vector of raw contents (each wrapped as an untitled Panel). Each panel's content is a single SVG path — a frame sequence is not allowed (storyboard is static; use montage for movies). Titles are where timestamps go.\n\nKeyword Arguments\n\nbackend::Symbol=:svg: :svg (default) or :makie (requires using CairoMakie).\nncols::Integer=length(panels): columns in the strip; the default is a single row.\npanel_width, title_height, pad, legend, legend_position, legend_font_size: as in montage. A single-row strip has no spare grid cell, so legend_position=:auto lands the legend in a full-width band below the frames.\noutput::Union{Nothing,AbstractString}=\"storyboard.svg\": where to write the SVG (cwd by default); errors if it exists unless overwrite=true. output=nothing returns the SVG string without writing.\noverwrite::Bool=false: allow writing over an existing output file.\n\nReturns\n\nThe composed SVG document as a String (written to output unless output=nothing).\n\nExamples\n\nusing Montage\n\n# A four-frame time strip with timestamp titles → writes ./storyboard.svg\nstoryboard([Panel(\"t0.svg\"; title=\"t = 0\"),   Panel(\"t1.svg\"; title=\"t = 120\"),\n            Panel(\"t2.svg\"; title=\"t = 240\"), Panel(\"t3.svg\"; title=\"t = 360\")])\n\n\n\n\n\n","title":"Montage.storyboard"},{"category":"method","location":"reference/#Montage.tableau-Tuple","page":"API reference","text":"tableau(args...; kwargs...)\n\nCompose one state as a focal panel surrounded by satellite panels — the verb for showing how the heterogeneous parts of a single state relate spatially (e.g. a cell layer centered with substrate heatmaps arranged around it, sharing a spatial extent).\n\nUnlike montage and storyboard, tableau is CairoMakie-only and data-driven — it re-plots from data rather than stitching SVGs — so it becomes available only once the CairoMakie extension is loaded (using CairoMakie); without it, calling tableau errors with that hint. Two methods are then provided:\n\ntableau(focal, satellites; …) — the data-agnostic form (MontageCairoMakieExt): focal and satellites are axis callbacks (ax -> …); it arranges them and writes the figure.\ntableau(::Type{Simulation}, sim_id; …) — the PhysiCell convenience form (MontageCairoMakiePCMMExt, also needs PhysiCellModelManager), which builds those callbacks from a simulation's cells + substrates.\n\n\n\n\n\n","title":"Montage.tableau"},{"category":"method","location":"reference/#Montage.montage-Tuple{AbstractVector{<:PhysiCellSequence}}","page":"API reference","text":"montage(seqs::AbstractVector{<:PhysiCellSequence}; index=:final, title=(<run name>), kwargs...)\nmontage(seq::PhysiCellSequence; …)\nmontage(snaps::AbstractVector{<:PhysiCellSnapshot}; …)\n\nCompose a montage across PhysiCell output folders (PhysiCellOutput extension). Like montage(::Type{Simulation}, …), the index value decides still vs. movie: a single :final/:initial/Integer → a still grid of that state; :all or a vector/range of snapshot indices → a movie (each panel's snapshot series, in lockstep). title is a function of the PhysiCellSequence.\n\nA cell-type legend is included by default (legend=:auto), built from each run's output/legend.svg — which lists every cell type the config defines, with PhysiCell's own colours — and drawn as flat circles and labels. It goes in the free cells trailing the last row when the grid has some (costing no space), else in a band below. Pass legend=nothing to suppress it, legend=[(\"label\", \"red\"), …] to give your own entries, or legend=\"path.svg\" to nest a hand-made file — and legend_position (:auto, :bottom, :top, (row, col)) to place whichever of those you chose. In a movie the legend is drawn into every frame.\n\ncell_types (a name or vector of names) and include_dead=false restrict which cells are drawn, by dropping the non-matching cell groups from each snapshot SVG — no re-rendering, and PhysiCell's \"N agents\" label is corrected to the number actually shown. A legend=:auto legend narrows to the kept types; an explicit legend — your own entries, or a file — is used exactly as given.\n\nAll other keywords pass through to the core verb (output, overwrite, panel_width, framerate, …).\n\n\n\n\n\n","title":"Montage.montage"},{"category":"method","location":"reference/#Montage.storyboard-Tuple{PhysiCellSequence}","page":"API reference","text":"storyboard(seq::PhysiCellSequence; index=nothing, n_snapshots=…, title=(t -> \"t = $t\"),\n           ncols=nothing, kwargs...)\n\nA static filmstrip of one PhysiCell output folder over time (PhysiCellOutput extension). Timepoints via index (a vector of snapshot indices and/or :initial/:final) or n_snapshots (default 4, evenly spaced incl. endpoints). Frame titles are the snapshot times through title.\n\ncell_types and include_dead restrict which cells are drawn, as in montage.\n\nA cell-type legend is included by default (legend=:auto), from the run's output/legend.svg, narrowed to the kept cell types (an explicit legend is used as given). Since a filmstrip is a single row with no spare cell, it lands in a band below; pass legend=nothing to suppress it. See montage for the other legend forms.\n\n\n\n\n\n","title":"Montage.storyboard"},{"category":"method","location":"reference/#Montage.montage-Tuple{Type{Simulation}, Any}","page":"API reference","text":"montage(::Type{Simulation}, sim_ids; index=:final, title=(id -> \"Sim $id\"),\n        panel_width=300, title_height=34, pad=12,\n        output=<auto: montage.svg | montage.mp4>, overwrite=false, framerate=15)\n\nCompose a montage from PhysiCell simulations (PCMM extension). Pass the simulation ids explicitly — e.g. montage(Simulation, simulationIDs()) to include every simulation. A single Integer id also works (a one-panel montage) — handy as montage(Simulation, id; index=:all) for a single-simulation movie.\n\nFor convenience you can also pass PCMM objects directly, and their constituent simulations are used: a trial (Simulation/Monad/Sampling/Trial), a run output (PCMMOutput), or a vector of either — e.g. montage(out) or montage([monad1, monad2]; index=:all).\n\nThe index value decides still image vs. movie:\n\nStill image — index is :final, :initial, or an Integer snapshot: one panel per simulation. Simulations missing the file are skipped with a warning. Writes an SVG.\nMovie — index is :all or a vector/range of snapshot indices (e.g. 0:5:120): each panel plays that simulation's snapshot sequence in lockstep. Writes a video via record (needs using Rsvg, Cairo, FFMPEG).\n\noutput defaults to dataDir()/outputs/montage.svg (still) or …/montage.mp4 (movie); it errors if the file exists unless overwrite=true. output=nothing returns the in-memory result (SVG string, or a MontageSpec for a movie). framerate applies to movies.\n\nExamples\n\nusing PhysiCellModelManager, Montage\n\nmontage(Simulation, simulationIDs())                        # final-state grid of every sim\nmontage(Simulation, [1, 2, 3]; index=:initial)              # initial states of sims 1–3\nmontage(Simulation, [1, 2, 3]; index=10, output=nothing)    # 10th snapshot, as an SVG string\n\nusing Rsvg, Cairo, FFMPEG                                   # movie extension\nmontage(Simulation, [1, 2, 22, 32]; index=:all, output=\"compare.mp4\", framerate=15)\n\n\n\n\n\n","title":"Montage.montage"},{"category":"method","location":"reference/#Montage.storyboard-Tuple{Type{Simulation}, Integer}","page":"API reference","text":"storyboard(::Type{Simulation}, sim_id; index=nothing, n_snapshots=…, title=(t -> \"t = $t\"),\n           ncols=nothing, panel_width=300, title_height=34, pad=12,\n           output=joinpath(dataDir(), \"outputs\", \"storyboard.svg\"), overwrite=false)\n\nStitch one simulation's time evolution into a static filmstrip (PCMM extension), each frame titled with its timestamp. sim_id is an Integer; you can also pass the simulation object (storyboard(sim)) or a single-simulation run output (storyboard(out) where out isa PCMMOutput{Simulation}).\n\nChoose the timepoints one of two ways:\n\nindex — a vector of snapshot selectors, each an Integer snapshot index or :initial/:final, e.g. [:initial, 30, 60, :final].\nn_snapshots (default 4) — that many evenly-spaced snapshots spanning the run, including the endpoints.\n\nPass one of them: n_snapshots defaults to length(index) when index is given; passing both with different lengths errors.\n\nTitles come from each frame's simulation time via title (default t -> \"t = $t\"). output defaults under dataDir()/outputs, errors if it exists unless overwrite=true, and output=nothing returns the SVG string. ncols defaults to a single row.\n\nExamples\n\nusing PhysiCellModelManager, Montage\n\nstoryboard(Simulation, 1)                                  # 4 evenly-spaced frames\nstoryboard(Simulation, 1; n_snapshots=6)                   # 6 frames\nstoryboard(Simulation, 1; index=[:initial, 30, 60, :final])\nstoryboard(Simulation, 1; title=t -> \"$(round(t/1440; digits=1)) d\")   # custom timestamp\n\n\n\n\n\n","title":"Montage.storyboard"},{"category":"method","location":"reference/#Montage.tableau-Tuple{Function, AbstractVector}","page":"API reference","text":"tableau(focal, satellites; focal_title=\"\", satellite_titles=[], colorbar_labels=[],\n        focal_colorbar_label=nothing, xlims=nothing, ylims=nothing, legend=:auto,\n        size=(1000, 1000), output=\"tableau.png\", overwrite=false)\n\nData-agnostic tableau (CairoMakie extension): a focal panel centered with satellites auto-ringed around it, each satellite paired with a colorbar, all sharing xlims/ylims.\n\nfocal is an axis callback ax -> …; satellites is a vector of callbacks ax -> plot, each returning the plot its colorbar reads (e.g. a heatmap!). satellite_titles and colorbar_labels are parallel to satellites. legend (built from the focal axis's labeled plots) is :auto (an empty grid cell, else an in-axis corner), a position Symbol (:rt, :lt, …), a grid cell (row, col), or nothing. Writes to output by default (erroring if it exists unless overwrite=true); output=nothing returns the Figure.\n\nSet focal_colorbar_label to give the focal panel a colorbar too — for a focal plot that encodes a continuous value rather than discrete categories. focal must then return its plot, the same convention the satellites already follow. Such a plot has no labeled series, so pass legend=nothing alongside it.\n\nExample\n\nusing CairoMakie, Montage\ntableau(ax -> scatter!(ax, xs, ys),\n        [ax -> heatmap!(ax, X, Y, Z)]; satellite_titles=[\"field\"], output=\"scene.png\")\n\n\n\n\n\n","title":"Montage.tableau"},{"category":"method","location":"reference/#Montage.tableau-Tuple{PhysiCellSequence}","page":"API reference","text":"tableau(seq::PhysiCellSequence; index=:final, substrates=<all>, colormap=:viridis,\n        markersize=6, color=:cell_type_name, color_mode=:auto, cell_colormap=:viridis,\n        cell_types=nothing, include_dead=true, legend=:auto, size=(1000, 1000),\n        framerate=15, output=<tableau.png | tableau.mp4>, overwrite=false)\n\nTableau of a PhysiCell output folder. index decides still vs. movie, as in montage: :final/:initial/Integer → a still of that state; :all or a vector/range of snapshot indices → a movie of the scene over those snapshots (fixed colorscale per substrate, stable legend). Writes tableau.png/tableau.mp4 by default.\n\ncolor, color_mode, cell_colormap, cell_types and include_dead control the cell layer — see tableau(::PhysiCellSnapshot). In a movie a continuous color gets a globally fixed colorrange (its min/max across every frame, computed after filtering) so the cell colorscale is comparable frame to frame, exactly as the substrate heatmaps already are.\n\n\n\n\n\n","title":"Montage.tableau"},{"category":"method","location":"reference/#Montage.tableau-Tuple{PhysiCellSnapshot}","page":"API reference","text":"tableau(snap::PhysiCellSnapshot; substrates=<all>, colormap=:viridis, markersize=6,\n        color=:cell_type_name, color_mode=:auto, cell_colormap=:viridis,\n        cell_types=nothing, include_dead=true,\n        legend=:auto, size=(1000, 1000), output=\"tableau.png\", overwrite=false)\n\nStill tableau of one PhysiCell state: the cells scattered, centered, with a substrate heatmap + colorbar per substrate around them. Reads the snapshot's data (reloading if the snapshot was constructed without it).\n\ncolor picks the cell column that sets the colour — :cell_type_name (the default) gives one labelled series per type plus a legend; a numeric column such as :pressure gives a single scatter with cell_colormap plus a colorbar. color_mode (:auto, :categorical, :continuous) overrides that choice for numeric-but-discrete columns like :current_phase. cell_types and include_dead restrict which cells are drawn. See cellLabels(snap) for the available columns.\n\n\n\n\n\n","title":"Montage.tableau"},{"category":"method","location":"reference/#Montage.tableau-Tuple{Type{Simulation}, Integer}","page":"API reference","text":"tableau(::Type{Simulation}, sim_id; index=:final, substrates=<all>, colormap=:viridis,\n        markersize=6, legend=:auto, size=(1000, 1000), framerate=15,\n        output=<dataDir()/outputs/tableau.png|.mp4>, overwrite=false)\n\nCompose one PhysiCell simulation state (CairoMakie + PhysiCellModelManager extension): the cell layer re-plotted as a scatter colored by cell type, centered, with one substrate heatmap + colorbar per substrate auto-arranged around it, all sharing a spatial extent.\n\nThe index value decides still image vs. movie, the same way as montage:\n\n:final/:initial/an Integer (a single snapshot selector) → a still;\n:all or a vector/range of snapshot indices → a movie: the whole scene animated over those snapshots via Makie.record, with a stable colorscale (each substrate's colorrange is fixed to its global min/max across frames) and a stable cell-type legend.\n\nOther kwargs: substrates (which get satellites, default all), colormap, markersize, size, and framerate (movies). output writes by default — tableau.png for a still (.svg/.pdf also work) or tableau.mp4 for a movie — erroring if it exists unless overwrite=true; for a still, output=nothing returns the Makie Figure (a movie needs a path). A Simulation object or a PCMMOutput{Simulation} is also accepted for sim_id.\n\nThe cell layer is configurable: color selects the cell column that sets the colour (:cell_type_name by default → one labelled series per type plus a legend; a numeric column such as :pressure → a colormap plus a colorbar), color_mode forces categorical vs. continuous, cell_colormap is the cell ramp (separate from colormap, which is the substrates'), and cell_types/include_dead restrict which cells are drawn. See tableau(::PhysiCellSnapshot).\n\nThis is a thin adapter over the folder-path tableau — it resolves the simulation's output folder and delegates.\n\nExamples\n\nusing CairoMakie, PhysiCellModelManager, Montage\ntableau(Simulation, 1)                                        # still: cells + every substrate\ntableau(Simulation, 1; index=60, substrates=[\"oxygen\"], legend=:rt, output=\"ox.png\")\ntableau(Simulation, 1; index=:all, output=\"tableau.mp4\", framerate=15)   # movie over all snapshots\ntableau(Simulation, 1; index=0:5:120)                         # movie over a subset\n\n\n\n\n\n","title":"Montage.tableau"},{"category":"section","location":"dev/journal/#Journal","page":"Journal","text":"<!– Generated by docs/journal.jl. Edit the !!! tierjournal      blocks on the source pages, not this file. –>","title":"Journal"},{"category":"section","location":"dev/journal/#2026-09-21-—-PCMM-0.4-brings-PhysiCellOutput-along","page":"Journal","text":"The PhysiCellModelManager extension is triggered by PCMM and PhysiCellOutput together, because it uses both. PhysiCellModelManager 0.4 took PhysiCellOutput as a dependency, so loading PCMM now loads PhysiCellOutput too and the trigger is satisfied without the user naming it — verified on 0.4.0 and 0.5.1. The compat bound rose to \"0.4, 0.5\" to make that promise true rather than conditional: under 0.3 the extension needed a using PhysiCellOutput the reader had no way to guess.\n\nFrom man/extensions.md","title":"2026-09-21 — PCMM 0.4+ brings PhysiCellOutput along"},{"category":"section","location":"dev/journal/#2026-09-21-—-ncols-over-an-explicit-Layout-type","page":"Journal","text":"ncols alone, defaulting to the near-square ceil(sqrt(n)), was enough to express the grid shape people actually ask for (conditions by replicates, panels listed row-major). Rejected a richer layout object: it would have to be threaded through stills, MontageSpec and every movie frame for a shape that one integer already names.\n\nFrom man/montage.md","title":"2026-09-21 — ncols over an explicit Layout type"},{"category":"section","location":"dev/journal/#2026-08-05-—-a-Plots.jl-backend,-measured-and-declined","page":"Journal","text":"The motivation was a downstream app compiling to 1.63 GB against a 500 MB target. Measured three builds: no plotting stack at all 700 MB, plus Plots.jl 1.27 GB, the current CairoMakie 1.63 GB. The floor alone exceeds the target, so no plotting library could have met it; swapping buys 22% and Plots is itself a 570 MB dependency. Not worth maintaining a second backend.\n\nFrom dev/architecture.md","title":"2026-08-05 — a Plots.jl backend, measured and declined"},{"category":"section","location":"dev/journal/#2026-08-05-—-the-legend-comes-from-PhysiCell's-own-legend.svg","page":"Journal","text":"The run already writes one file listing every cell type the config defines, with the colours PhysiCell drew them in. Reading it costs one ~1.5 KB file per simulation and is independent of frame count, and because it describes what the model can contain rather than what one snapshot happens to show, a single legend is correct for every frame of a movie. Rejected scanning snapshots for the types actually present: it is per-frame work, and it produces a legend that changes as cells appear and die. The entries are drawn as flat circles and text rather than nested as an SVG so they stay editable in Illustrator and PowerPoint.\n\nFrom man/physicell.md","title":"2026-08-05 — the legend comes from PhysiCell's own legend.svg"},{"category":"section","location":"dev/journal/#2026-08-05-—-a-built-in-colormap-set-on-the-stitched-path","page":"Journal","text":"Colouring cells by a data value rewrites fills in the snapshot SVG, so it needs a colormap but not a plotting stack. Carrying :viridis, :plasma and :grays in the core keeps montage and storyboard dependency-free; tableau already re-plots through Makie and has the full set.\n\nFrom man/physicell.md","title":"2026-08-05 — a built-in colormap set on the stitched path"},{"category":"section","location":"dev/journal/#2026-08-05-—-PDF,-not-SVG,-for-a-publication-tableau","page":"Journal","text":"Measured one cell layer plus a 50x50 substrate at 154 KB (.png), 52 KB (.pdf) and 739 KB (.svg). PDF is vector AND stream-compressed, so it is the smallest of the three — the opposite of the \"vector costs you size\" caveat that was going to be written. The SVG bulk is ~2500 heatmap paths, not the cell scatter, so a finer mesh inflates it faster than more cells do.\n\nFrom man/tableau.md","title":"2026-08-05 — PDF, not SVG, for a publication tableau"},{"category":"section","location":"dev/journal/#2026-07-27-—-two-front-doors,-one-reader-path","page":"Journal","text":"PhysiCell support ships as Montage extensions rather than a separate PhysiCellMontage.jl package. The clunky name was not worth it, and extensions already decouple what a user types from where code lives — everyone still types using Montage. Folder paths go through MontagePhysiCellOutputExt, which holds the real logic; simulation ids go through MontagePhysiCellModelManagerExt, a thin adapter that resolves an id to a folder and delegates. Both read through PhysiCellOutput, so there is one reader path rather than two implementations that could drift.\n\nFrom man/extensions.md","title":"2026-07-27 — two front doors, one reader path"},{"category":"section","location":"dev/journal/#2026-07-22-—-tableau-is-CairoMakie-only","page":"Journal","text":"The other two verbs stitch existing SVGs, which cannot give real heatmaps, real colorbars or a shared spatial extent. Rejected forcing tableau into the SVG backend to keep it in the core; it would have produced a picture that looked like a tableau without the axes actually agreeing. CairoMakie stays a weak dependency, so the cost lands only on people who ask for this verb.\n\nFrom man/tableau.md","title":"2026-07-22 — tableau is CairoMakie-only"},{"category":"section","location":"dev/journal/#2026-07-21-—-SVG-string-stitching-as-the-core-default","page":"Journal","text":"The core stitches nested <svg> elements rather than re-plotting, which keeps the figure lossless vector, keeps PhysiCell's own styling exactly, and keeps the package free of any heavy dependency. Decided against making CairoMakie the default backend for this verb: it would force the whole Makie stack on every user for a grid of images it would only rasterize or redraw. The backend keyword and its MakieBackend selector remain as scaffolding; montage is SVG-only.\n\nFrom man/montage.md","title":"2026-07-21 — SVG string-stitching as the core default"},{"category":"section","location":"dev/journal/#2026-07-21-—-record(spec,-path)-rather-than-a-movie-keyword","page":"Journal","text":"A verb builds a composition; a separate entry point animates it. Rejected a movie= keyword on each verb and rejected _movie/_gif variants: both spread the same decision across every verb's signature, and neither leaves anywhere to put render-only options like scale.\n\nFrom man/movies.md","title":"2026-07-21 — record(spec, path) rather than a movie= keyword"},{"category":"section","location":"dev/journal/#2026-07-21-—-SVG-frames-plus-FFMPEG-for-montage-movies","page":"Journal","text":"Rasterizing each stitched frame through Rsvg/Cairo and encoding with FFMPEG preserves PhysiCell's own styling exactly. Rejected rebuilding the grid in Makie for this path: it would redraw renders that already exist, and it would pull the Makie stack into the most common movie.\n\nFrom man/movies.md","title":"2026-07-21 — SVG frames plus FFMPEG for montage movies"},{"category":"section","location":"man/montage/#montage-page","page":"Montage","text":"montage arranges homogeneous panels into a uniform titled grid — the verb for comparing like-for-like across many things, such as the final state of every simulation in a batch.\n\ntierfull: Tierfull\nThe grid is stitched, not re-plotted. Each panel's SVG is inlined into the composition as a nested, positioned <svg> element, so the result stays lossless vector at any size and every panel keeps the styling of whatever drew it. Nothing is rasterized and no plotting library is involved: a still montage needs no package beyond Montage itself.\n\ntierjournal: 2026-07-21 — SVG string-stitching as the core default\nThe core stitches nested <svg> elements rather than re-plotting, which keeps the figure lossless vector, keeps PhysiCell's own styling exactly, and keeps the package free of any heavy dependency. Decided against making CairoMakie the default backend for this verb: it would force the whole Makie stack on every user for a grid of images it would only rasterize or redraw. The backend keyword and its MakieBackend selector remain as scaffolding; montage is SVG-only.","title":"Montage"},{"category":"section","location":"man/montage/#Panels","page":"Montage","text":"tierbrief: Tierbrief\nA Panel is one cell of the composition: some content, plus an optional title drawn above it. Pass a Vector{Panel}, or a loose vector of raw contents, which is wrapped as untitled panels.\n\ntierfull: Tierfull\nFor the SVG backend a panel's content is a path to an SVG file. The file is read once, inlined, and scaled to its cell by a viewBox — so it fits instead of clipping, and it scales instead of pixellating. Its intrinsic size is parsed out of the root <svg> tag, never assumed, so panels rendered at different sizes compose fine.An empty title is the default, and a composition where no panel carries one reserves no title band at all.\n\nsvg = montage([Panel(a; title=\"A\"), Panel(b; title=\"B\")]; output=nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"636.0\\\" height=\\\"358.0\\\" viewBox=\\\"0 0 636.0 358.0\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"636.0\\\" height=\\\"358.0\\\" fill=\\\"white\\\"/>\\n<text x=\\\"162.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">A</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"tomato\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">B</text>\\n<svg x=\\\"324.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"steelblue\\\"/>\\n</svg>\\n</svg>\\n\")\n\ntierdev: Tierdev\nThe :svg backend requires each panel's content to be an AbstractString path to an existing file, and errors otherwise; there is no rendering fallback. A still panel's transform must be a single function — a Vector is the per-frame movie form and is rejected with a message saying so.","title":"Panels"},{"category":"section","location":"man/montage/#The-grid","page":"Montage","text":"tierbrief: Tierbrief\nPanels fill the grid row-major. ncols sets the number of columns and defaults to the near-square ceil(sqrt(n)).\n\ntierfull: Tierfull\nEvery cell is the same size: panel_width (default 300) px wide, and tall enough for the largest aspect ratio among the panels, so no panel overflows or clips. pad (default 12) px separates the cells and frames the figure. A title band of title_height (default 34) px is reserved for the whole grid only if at least one panel carries a title, so an all-untitled composition wastes no vertical space.Set ncols when the comparison itself has a shape. Listing panels row-major with ncols equal to the number of replicates puts one condition per row, which is the figure you were going to assemble by hand anyway.\n\ntitles = [\"control 1\", \"control 2\", \"low dose 1\", \"low dose 2\", \"high dose 1\", \"high dose 2\"]\nsvg = montage([Panel(p; title=t) for (p, t) in zip(reps, titles)];\n              ncols=2, panel_width=180, output=nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"396.0\\\" height=\\\"690.0\\\" viewBox=\\\"0 0 396.0 690.0\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"396.0\\\" height=\\\"690.0\\\" fill=\\\"white\\\"/>\\n<text x=\\\"102.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">control 1</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"180\\\" height=\\\"180.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"tomato\\\"/>\\n</svg>\\n<text x=\\\"294.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">control 2</text>\\n<svg x=\\\"204.0\\\" y=\\\"46.0\\\" width=\\\"180\\\" height=\\\"180.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"salmon\\\"/>\\n</svg>\\n<text x=\\\"102.0\\\" y=\\\"262.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">low dose 1</text>\\n<svg x=\\\"12.0\\\" y=\\\"272.0\\\" width=\\\"180\\\" height=\\\"180.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"steelblue\\\"/>\\n</svg>\\n<text x=\\\"294.0\\\" y=\\\"262.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">low dose 2</text>\\n<svg x=\\\"204.0\\\" y=\\\"272.0\\\" width=\\\"180\\\" height=\\\"180.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"skyblue\\\"/>\\n</svg>\\n<text x=\\\"102.0\\\" y=\\\"488.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">high dose 1</text>\\n<svg x=\\\"12.0\\\" y=\\\"498.0\\\" width=\\\"180\\\" height=\\\"180.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"seagreen\\\"/>\\n</svg>\\n<text x=\\\"294.0\\\" y=\\\"488.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">high dose 2</text>\\n<svg x=\\\"204.0\\\" y=\\\"498.0\\\" width=\\\"180\\\" height=\\\"180.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"darkseagreen\\\"/>\\n</svg>\\n</svg>\\n\")\n\ntierdev: Tierdev\nThe geometry lives in one place, _svgGrid in src/svg_backend.jl, which montage and storyboard share. The only difference between the two verbs is the column count they hand it: ceil(sqrt(n)) against n.\n\ntierjournal: 2026-09-21 — ncols over an explicit Layout type\nncols alone, defaulting to the near-square ceil(sqrt(n)), was enough to express the grid shape people actually ask for (conditions by replicates, panels listed row-major). Rejected a richer layout object: it would have to be threaded through stills, MontageSpec and every movie frame for a shape that one integer already names.","title":"The grid"},{"category":"section","location":"man/montage/#Output","page":"Montage","text":"tierbrief: Tierbrief\nmontage writes ./montage.svg and also returns the composed SVG string. Point output somewhere else, or pass output=nothing to get the string without writing anything. Writing an existing path errors unless overwrite=true.\n\nmontage(panels)                                   # ./montage.svg, and returns the SVG\nmontage(panels; output=\"figures/batch.svg\", overwrite=true)\nsvg = montage(panels; output=nothing)             # compose only, write nothing","title":"Output"},{"category":"section","location":"man/montage/#Legends","page":"Montage","text":"tierbrief: Tierbrief\nlegend says what to draw; legend_position says where it goes. They are independent, so any legend content can take any placement.\n\ntierfull: Tierfull\nlegend takes nothing (the default, no legend), a vector of (label, colour) entries to draw, a path or SVG string to nest as-is, or a function that draws one. Drawn entries come out as flat <circle> and <text> elements rather than a nested <svg>, which is what lets you open the figure in Illustrator or PowerPoint and nudge the legend around. legend_font_size sets their size and defaults to the panel-title size, so the two match; a drawn legend holds that size and wraps to fit whatever space it is given.legend_position takes :auto, :bottom, :top, or an explicit (row, col) or (row, col, span) cell. :auto prefers the free cells trailing the last row when the layout has any — seven panels in a three-wide grid leave two — so the legend costs no space at all; with no spare cells it falls back to a full-width band below.\n\nsvg = montage([Panel(a; title=\"A\"), Panel(b; title=\"B\")];\n              legend=[(\"tumour\", \"grey\"), (\"immune\", \"seagreen\")],\n              legend_position=:bottom, output=nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"636.0\\\" height=\\\"407.4\\\" viewBox=\\\"0 0 636.0 407.4\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"636.0\\\" height=\\\"407.4\\\" fill=\\\"white\\\"/>\\n<text x=\\\"162.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">A</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"tomato\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">B</text>\\n<svg x=\\\"324.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"steelblue\\\"/>\\n</svg>\\n<circle cx=\\\"209.54\\\" cy=\\\"376.7\\\" r=\\\"9.9\\\" stroke=\\\"black\\\" stroke-width=\\\"1\\\" fill=\\\"grey\\\"/>\\n<text x=\\\"228.24\\\" y=\\\"384.4\\\" font-family=\\\"Arial\\\" font-size=\\\"22.0\\\" fill=\\\"black\\\">tumour</text>\\n<circle cx=\\\"341.1\\\" cy=\\\"376.7\\\" r=\\\"9.9\\\" stroke=\\\"black\\\" stroke-width=\\\"1\\\" fill=\\\"seagreen\\\"/>\\n<text x=\\\"359.8\\\" y=\\\"384.4\\\" font-family=\\\"Arial\\\" font-size=\\\"22.0\\\" fill=\\\"black\\\">immune</text>\\n\\n</svg>\\n\")\n\ntierdev: Tierdev\nThe draw-function form has the signature (x, y, avail_w) -> (fragment, w, h) and is called more than once — to measure, then to emit — so it must be pure. It is how the PhysiCell extension supplies a colorbar that the core knows nothing about.","title":"Legends"},{"category":"section","location":"man/montage/#Editing-panel-contents:-the-transform-seam","page":"Montage","text":"tierbrief: Tierbrief\nPanel(content; transform=f) runs f, an SVG text -> SVG text function, on that panel's markup just before it is placed.\n\ntierfull: Tierfull\nConstructing a Panel does nothing on its own — the transform runs when the grid is built, on the text read from the file, so the source file is never modified. The default identity returns the very same object, which makes the untransformed path byte-identical to one with no transform support at all. Anything the transform injects is written in the panel's own coordinate system and scales with it, because each panel is nested with its intrinsic viewBox.For a movie panel, transform can be a single function applied to every frame, or a Vector of functions the same length as the frames, in which case frame t is edited by transform[t]. The per-frame form exists because some edits depend on the timepoint: colouring cells by a data value has to map each frame from that snapshot's own values.\n\n# stamp a scale bar into each panel on its way into the grid\nscalebar(svg) = replace(svg, r\"</svg>\\s*$\"s =>\n    \"\"\"<rect x=\"40\" y=\"940\" width=\"200\" height=\"10\" fill=\"black\"/></svg>\"\"\")\n\nmontage([Panel(\"control/final.svg\"; title=\"control\", transform=scalebar),\n         Panel(\"treated/final.svg\"; title=\"treated\", transform=scalebar)])","title":"Editing panel contents: the transform seam"},{"category":"section","location":"man/montage/#Comparing-dynamics","page":"Montage","text":"tierbrief: Tierbrief\nGive a panel a Vector of frame paths instead of one path and the same call produces a movie: every panel plays its frames in lockstep, aligned by index and truncated to the shortest sequence. See Movies for the rendering path, framerate, scale and record.\n\nusing Rsvg, Cairo, FFMPEG    # the movie extension\n\nmontage([Panel(control_frames; title=\"control\"),\n         Panel(treated_frames; title=\"treated\")];\n        output=\"compare.mp4\", framerate=15)","title":"Comparing dynamics"},{"category":"section","location":"man/montage/#PhysiCell-simulations","page":"Montage","text":"tierbrief: Tierbrief\nWith PhysiCellOutput or PhysiCellModelManager loaded, montage takes output folders (as a PhysiCellSequence) and simulation ids directly, and gains cell-type filtering, colouring cells by a data column, and an automatic legend read from each run's own legend.svg. PhysiCell simulations covers all of it.\n\nusing PhysiCellModelManager, Montage\n\nmontage(Simulation, [1, 2, 3]; index=:final)","title":"PhysiCell simulations"},{"category":"section","location":"man/movies/#movies-page","page":"Movies","text":"A movie is the same composition as a still, given frames instead of a single image: you build the composition with a verb, and what the content is decides whether it renders a picture or a video.\n\ntierbrief: Tierbrief\nTwo verbs animate. montage turns into a movie when its panels carry frame sequences; tableau turns into a movie when its data source spans several timepoints. storyboard never animates — it is the filmstrip, laid out in space instead of time.\n\ntierfull: Tierfull\nThere is no movie=true keyword and no montage_movie/montage_gif companion function. A keyword would have to be repeated on every verb, and a companion function would duplicate every verb's argument list; both leave render-only options such as scale and framerate with nowhere natural to live. Instead a verb builds a composition and rendering is a separate step — implicit in the one-call form, explicit in record.","title":"Movies"},{"category":"section","location":"man/movies/#Montage-of-movies","page":"Movies","text":"Give each panel a Vector of frame paths instead of one path, and montage writes a video.\n\ntierbrief: Tierbrief\nThe renderer lives in a package extension: using Rsvg, Cairo, FFMPEG loads it. Without those three, building the composition still works and only the render step errors, with a message naming them.\n\ntierfull: Tierfull\nPanels play in lockstep: frame t of the movie is the ordinary stitched grid of every panel's t-th frame. Frames are matched by index, not by simulation time, and the movie is truncated to the shortest sequence — a warning reports the per-panel counts when they differ. Every panel must carry a sequence; mixing a single-image panel into an animated montage is an error rather than a frozen tile.The default output follows the content: montage.mp4 once any panel is animated, montage.svg otherwise. Writing refuses to clobber an existing file unless you pass overwrite=true.\n\nusing Montage, Rsvg, Cairo, FFMPEG\n\nmontage([Panel([\"a/snapshot1.svg\", \"a/snapshot2.svg\", \"a/snapshot3.svg\"]; title=\"A\"),\n         Panel([\"b/snapshot1.svg\", \"b/snapshot2.svg\", \"b/snapshot3.svg\"]; title=\"B\")];\n        output=\"compare.mp4\", framerate=15)","title":"Montage of movies"},{"category":"section","location":"man/movies/#Rendering-in-two-steps","page":"Movies","text":"Pass output=nothing and the movie is not rendered: you get a MontageSpec, which record turns into a video whenever you like.\n\ntierbrief: Tierbrief\nrecord takes framerate and overwrite just as the one-call form does, plus the one option only it has: scale, the rasterization factor. scale=2 renders every frame at twice the composition's nominal pixel size, which is what you want when a 300 px panel is going onto a projector; it costs encode time and file size and nothing else.\n\ntierfull: Tierfull\nThe spec carries the panels, the frame count, the grid geometry and the legend, so rendering it needs nothing back from the verb — only the frame files its panels name, which are read as each frame is composed. A legend supplied as a file is read into the spec at that moment rather than re-read per frame, and the same legend is drawn into every frame — it cannot flicker, and it cannot disappear because a category is absent from one timepoint.Each panel's transform comes along too (see Panel). One function edits every frame, which suits a time-independent edit like dropping a cell type; a Vector of functions the same length as the frames edits frame t with transform[t], which is what colouring by a value that changes over time requires.\n\nframes(dir) = sort(filter(endswith(\".svg\"), readdir(dir; join=true)))\n\nspec = montage([Panel(frames(\"a\"); title=\"A\"), Panel(frames(\"b\"); title=\"B\")];\n               output=nothing)\nrecord(spec, \"compare.mp4\"; framerate=24, scale=2)\n\ntierdev: Tierdev\nmontage builds the spec in Montage._montageSpec (src/movie.jl), which validates the panels and resolves the legend; Montage._svgFrame(spec, t) composes one frame by re-entering the same _svgGrid the still path uses, with each panel's t-th frame and Montage._frameTransform(p.transform, t). record itself does only the non-clobber check (Montage._assertWritable) and delegates to Montage._recordSVGMovie, whose fallback method in src/movie.jl only errors; the real method arrives with MontageMovieExt (ext/MontageMovieExt.jl).\n\ntierjournal: 2026-07-21 — record(spec, path) rather than a movie= keyword\nA verb builds a composition; a separate entry point animates it. Rejected a movie= keyword on each verb and rejected _movie/_gif variants: both spread the same decision across every verb's signature, and neither leaves anywhere to put render-only options like scale.","title":"Rendering in two steps"},{"category":"section","location":"man/movies/#How-a-frame-is-made","page":"Movies","text":"The container comes from the path's extension.\n\ntierbrief: Tierbrief\nEach timepoint is composed as an ordinary stitched montage SVG, rasterized to a PNG through Rsvg and Cairo, and the resulting sequence is encoded by FFMPEG. .gif is written straight by FFMPEG; anything else is encoded as H.264 with yuv420p pixels.\n\ntierfull: Tierfull\nBecause each frame is the source SVGs stitched and then rasterized, the movie keeps their styling exactly — for PhysiCell output, the same cell colours, outlines and text its own renders carry. Nothing is re-plotted, so nothing can drift between a still montage and the movie of the same runs.scale multiplies the rasterization only. The composition's geometry — panel width, padding, title band, legend — is fixed in the spec, so a scaled movie is the same figure at more pixels, not a different layout.\n\nrecord(spec, \"compare.gif\")                    # FFMPEG's GIF path\nrecord(spec, \"compare.mp4\"; scale=2)           # H.264, yuv420p\nrecord(spec, \"compare.mp4\"; overwrite=true)    # replace an existing file\n\ntierdev: Tierdev\nMontageMovieExt._rasterizeSVG rounds the raster up to even width and height (yuv420p encoders reject odd dimensions) and paints a white backdrop first, since an ARGB32 surface starts transparent and the even-dimension padding would otherwise show. Frames are written to a mktempdir that a finally removes whether or not the encode succeeds.\n\ntierjournal: 2026-07-21 — SVG frames plus FFMPEG for montage movies\nRasterizing each stitched frame through Rsvg/Cairo and encoding with FFMPEG preserves PhysiCell's own styling exactly. Rejected rebuilding the grid in Makie for this path: it would redraw renders that already exist, and it would pull the Makie stack into the most common movie.","title":"How a frame is made"},{"category":"section","location":"man/movies/#Tableau-movies","page":"Movies","text":"A tableau animates when its data source covers several timepoints — for PhysiCell output, index=:all or a range of snapshot indices.\n\ntierbrief: Tierbrief\nThis path needs using CairoMakie plus a data door: PhysiCellOutput for an output folder, PhysiCellModelManager for a simulation id. The whole scene animates — the cell layer and every substrate heatmap together — and the result must go to a file in a video container, so output=nothing is an error here.\n\ntierfull: Tierfull\nEach substrate's colorscale is fixed across the movie from that substrate's minimum and maximum over every frame, so a colour means one concentration from the first frame to the last. The cell layer is stabilised the same way: a categorical colouring draws the union of values seen in any frame, in a fixed order, so the legend and the colours never reshuffle; a continuous colouring gets one globally fixed colorrange, computed after filtering. Between frames only the data and the timestamp in the focal panel's title change, never the axis limits, the legend or the colorbars.\n\nusing Montage, CairoMakie, PhysiCellOutput\n\nseq = PhysiCellSequence(\"path/to/output\")\ntableau(seq; index=:all, output=\"scene.mp4\", framerate=15)\ntableau(seq; index=0:5:120, substrates=[\"oxygen\"], output=\"oxygen.mp4\")\n\ntierdev: Tierdev\n_tableauMovie in ext/MontageCairoMakiePhysiCellOutputExt.jl loads every requested snapshot up front, builds the figure once through the generic tableau(focal, satellites; output=nothing) with Observables behind the heatmaps, the scatter and the focal title, then drives CairoMakie.record. In the continuous case the positions and the colour values travel in a single Observable holding both: updating two would leave one frame where the vectors have different lengths.","title":"Tableau movies"},{"category":"section","location":"man/movies/#Two-engines,-one-idea","page":"Movies","text":" montage movie tableau movie\nSource finished SVG renders, stitched data, re-plotted\nEngine Rsvg + Cairo rasterization, FFMPEG encode Makie.record\nLoad using Rsvg, Cairo, FFMPEG using CairoMakie, plus PhysiCellOutput or PhysiCellModelManager\nEntry point record on a MontageSpec tableau with a multi-timepoint index\n\ntierfull: Tierfull\nThe split follows from what each verb composes. A montage assembles renders that already exist, so the cheapest correct movie is to keep assembling them and hand the frames to an encoder — pulling in a plotting stack would only redraw finished pictures. A tableau has no finished render to assemble: it draws the scatter and the heatmaps itself from the cells and substrate tables, so animating it means holding that figure and swapping its data, which is exactly Makie.record.One consequence is worth knowing before you choose: a montage movie is available with no Makie stack loaded, and a tableau movie is the only path that gives you real heatmaps and colorbars. See Extensions for what each set of packages unlocks.","title":"Two engines, one idea"},{"category":"section","location":"man/movies/#Driving-movies-from-simulations","page":"Movies","text":"With PhysiCellModelManager loaded, both movie paths take simulation ids directly.\n\nusing Montage, PhysiCellModelManager, Rsvg, Cairo, FFMPEG\n\nmontage(Simulation, [1, 2, 3]; index=:all, output=\"compare.mp4\", framerate=15)\n\nSnapshot selection, cell filtering, colouring and the automatic cell-type legend are covered in PhysiCell simulations.","title":"Driving movies from simulations"},{"category":"section","location":"dev/architecture/#architecture-page","page":"Architecture","text":"Where to make a change: what lives in which file, how one call travels through them, what must not break, and how to run the tests and the docs.","title":"Architecture"},{"category":"section","location":"dev/architecture/#Module-map","page":"Architecture","text":"tierbrief: Tierbrief\nThe core holds every verb, every type and the SVG stitching backend, and takes no heavy dependency. The six extensions add methods to names the core already owns.\n\nsrc/ Holds\nsrc/Montage.jl The module. Includes the files below in order and exports Panel, MontageSpec, montage, storyboard, tableau, record.\nsrc/types.jl Panel, MontageSpec, the backend selectors (MontageBackend, SVGBackend, MakieBackend, montageBackend), _asPanels, _isAnimated, _frameTransform, _TITLE_FONT_SIZE.\nsrc/svg_backend.jl The string-stitch backend: _svgDimensions, _nestedSVG, _svgSource, _svgGrid, the legend machinery (_normalizeLegend, _legendLayout, _svgLegend), and the core-declared _cellTypeLegend hook.\nsrc/montage_verb.jl The montage verb, _resolvedNcols, _writeSVG, _assertWritable. Named montage_verb.jl because macOS's case-insensitive filesystem would collide a montage.jl with Montage.jl.\nsrc/movie.jl _montageSpec, _svgFrame, the record entry point, and the _recordSVGMovie fallback that errors without the movie extension.\nsrc/storyboard.jl The storyboard verb, which rejects animated panels.\nsrc/tableau.jl Declares tableau; the core method only errors with a \"load CairoMakie\" hint.\n\next/ Triggers Adds\next/MontageMovieExt.jl Rsvg, Cairo, FFMPEG _rasterizeSVG and the concrete Montage._recordSVGMovie: rasterize each stitched frame, encode with FFMPEG.\next/MontagePhysiCellOutputExt.jl PhysiCellOutput montage on PhysiCellSequence and PhysiCellSnapshot, storyboard on PhysiCellSequence: snapshot globbing, _cellTransform filtering and recolouring, legend.svg parsing, _colorPlan, _svgColorbar.\next/MontagePhysiCellModelManagerExt.jl PhysiCellModelManager, PhysiCellOutput montage/storyboard by simulation id, trial or PCMMOutput. Knows _outputFolder and nothing else; delegates.\next/MontageCairoMakieExt.jl CairoMakie The layout engine: _ringSlots, _tableauFigure, _placeLegend!, and the data-agnostic tableau(focal, satellites).\next/MontageCairoMakiePhysiCellOutputExt.jl CairoMakie, PhysiCellOutput Data-driven tableau of a snapshot or folder: _substrateGrid, _cellMask, _physiCellPalette, _tableauStill, _tableauMovie.\next/MontageCairoMakiePCMMExt.jl CairoMakie, PhysiCellModelManager, PhysiCellOutput tableau by simulation id, Simulation object or PCMMOutput{Simulation}. Resolves the folder and delegates.\n\ntierdev: Tierdev\nTwo file pairs look redundant and are not. MontagePhysiCellOutputExt holds the PhysiCell logic and MontagePhysiCellModelManagerExt is a thin adapter over it; the same split repeats for tableau across MontageCairoMakiePhysiCellOutputExt and MontageCairoMakiePCMMExt. Put new PhysiCell behaviour in the folder extension and both doors get it.montageBackend(:makie) returns a MakieBackend() that no method implements, so montage/storyboard with backend=:makie reach the abstract-type fallbacks _montage/_storyboard(::MontageBackend, …) and error. Those two verbs stitch SVG; tableau is the CairoMakie verb and dispatches on its arguments, not on a backend selector.\n\ntierjournal: 2026-08-05 — a Plots.jl backend, measured and declined\nThe motivation was a downstream app compiling to 1.63 GB against a 500 MB target. Measured three builds: no plotting stack at all 700 MB, plus Plots.jl 1.27 GB, the current CairoMakie 1.63 GB. The floor alone exceeds the target, so no plotting library could have met it; swapping buys 22% and Plots is itself a 570 MB dependency. Not worth maintaining a second backend.","title":"Module map"},{"category":"section","location":"dev/architecture/#The-data-flow-of-one-call","page":"Architecture","text":"tierbrief: Tierbrief\nFollow one movie call from a simulation id — or from the trial object that resolves to one — to an encoded file. Every hop below is a method on a name the core owns.\n\nusing Montage, PhysiCellModelManager, Rsvg, Cairo, FFMPEG\n\nmontage(Simulation, ids; index=:all, output=\"compare.mp4\")   # from ids\nmontage(sampling; index=:all, output=\"compare.mp4\")          # or from the trial itself\n\n# Function File\n0 montage(::AbstractTrial) / montage(::PCMMOutput), and vectors of either, through simulationIDs — the hop you skip by passing ids ext/MontagePhysiCellModelManagerExt.jl\n1 montage(::Type{Simulation}, sim_ids; …), through _outputFolder and _sequence ext/MontagePhysiCellModelManagerExt.jl\n2 montage(::AbstractVector{<:PhysiCellSequence}; …), through _frameIndices, _frameSVGs, _colorPlan, _legendFor ext/MontagePhysiCellOutputExt.jl\n3 montage(panels; …), through _asPanels and _normalizeLegend src/montage_verb.jl\n4 _montageSpec, building the MontageSpec src/movie.jl\n5 record(spec, output; framerate, overwrite), through _assertWritable src/movie.jl\n6 _recordSVGMovie: _rasterizeSVG per frame, then FFMPEG.exe ext/MontageMovieExt.jl\n7 _svgFrame(spec, t) per frame, which calls _svgGrid src/movie.jl, src/svg_backend.jl\n\ntierdev: Tierdev\nHop 0 is one line per method: it resolves whatever PCMM handed you — a Simulation, Monad, Sampling or Trial, a PCMMOutput, or a vector of either — into simulation ids with simulationIDs, then calls hop 1. Everything downstream is identical, which is the point: the object forms are a convenience over the id form, not a second path.Hop 1 turns each id into trialFolder(Simulation, id)/output and wraps it in a PhysiCellOutput.PhysiCellSequence, keeping a folder-to-id map so the caller's title function still sees ids. Hop 2 is where PhysiCell knowledge lives: it globs the snapshot SVG paths, asks _colorPlan for one transform per panel (a Vector of them, one per frame, when color is set), and resolves legend=:auto into (label, colour) entries from each run's legend.svg — or into a colorbar draw function when cells are recoloured by data. It then builds Panels whose content is a Vector of frame paths and hands them to the core verb.Hop 3 sees animated panels and branches to the spec builder rather than the still backend. Hop 4 truncates to the shortest frame sequence (warning when the counts differ) and resolves a legend file to text once, so no frame re-reads it. Hop 6 raster-pads each frame to even dimensions, which is what yuv420p H.264 needs, and writes a GIF instead when the path ends in .gif. Hop 7 reuses the static grid logic unchanged, indexing each panel's transform by frame.The still path (index=:final) diverges at hop 2, which builds one-image panels — warning past any folder missing that snapshot — so hop 3 takes _montage(::SVGBackend, …) straight to _svgGrid and _writeSVG, and hops 4 to 7 never run: no spec, no rasterizer, no FFMPEG.","title":"The data flow of one call"},{"category":"section","location":"dev/architecture/#Invariants","page":"Architecture","text":"Invariant What breaks if it goes\nEvery verb, type and selector is defined in src/; extensions only add methods. An extension cannot introduce a name its parent lacks, and extensions cannot using one another. A verb declared in an extension is unreachable from using Montage, and a helper two extensions share has nowhere to live — _cellTypeLegend is declared in core for exactly that reason.\nA core fallback is deliberately more general than the method an extension adds (_recordSVGMovie, _montage(::MontageBackend, …)). Matching signatures make the extension's method an overwrite, which Julia rejects during precompilation. _cellTypeLegend takes the other escape: declared with no methods at all.\nCairoMakie, PhysiCellOutput, PhysiCellModelManager, Rsvg, Cairo and FFMPEG stay in [weakdeps]. using Montage drags in a plotting stack and the SVG default stops being the light path. The test target's [extras] is Test alone for the same reason.\nIntrinsic SVG sizes are parsed by _svgDimensions, never hardcoded. PhysiCell's final.svg happens to be 1000×1070. Panels clip or stretch the moment a source renders at another size: a different domain, a 3-D run, an SVG from another tool.\nPanel's default transform is identity, and identity stays free. identity returns the very same string object, so an untransformed composition is byte-identical to one built with no transform support. Anything that rewrites panel text unconditionally costs every panel and forfeits that.\nTitles and drawn legends are emitted as real <text> and flat <circle> markers. The composed figure stops being editable in Illustrator or PowerPoint, which is the reason to stitch vector SVG at all. CairoMakie's .svg converts text to glyph outlines, so it is not a substitute.\nPhysiCellOutput is the single reader path, and the PCMM extension stays a thin adapter over it. Two readers drift, and the same run renders differently depending on which door the user came through.\nA color range is pooled across every panel and every frame, and computed after cell_types/include_dead filtering (_colorPlan). A colour stops meaning the same thing across the figure, which is the one job a montage has; cells that are never drawn stretch the scale.\nEvery write goes through _assertWritable. Nothing stands between a re-run and an overwritten figure.","title":"Invariants"},{"category":"section","location":"dev/architecture/#Running-the-tests","page":"Architecture","text":"julia --project=. -e 'using Pkg; Pkg.test()'\njulia --project=. test/runtests.jl\n\ntierdev: Tierdev\nThe second form runs the same file outside Pkg's sandbox, which is faster to iterate on. test/runtests.jl reads no arguments, so there is no filter flag: to run a single block, paste it into a REPL that has using Montage, Test. Testsets are named \"<area> — <behaviour>\" (\"legend — drawn entries\", \"Panel transform — per-frame in a movie\") and nested under one top-level @testset \"Montage.jl\". Each block builds its own fixtures from the SVG string constants at the top of the file, most through the withtmpsvgs helper, so no block depends on another having run.The suite depends on Test and nothing else: no PhysiCellModelManager, no PhysiCellOutput, no CairoMakie, no external data. It exercises the SVG backend on tiny hand-written SVGs of differing intrinsic sizes, which is what pins the max-aspect grid logic, the legend placement rules and the byte-identical guarantees. That means it cannot catch broken [weakdeps] or [extensions] wiring — the docs build does, because it loads all six.","title":"Running the tests"},{"category":"section","location":"dev/architecture/#Building-the-docs","page":"Architecture","text":"julia --project=docs -e 'using Pkg; Pkg.develop(path=\".\"); Pkg.instantiate()'\njulia --project=docs docs/make.jl\n\ntierdev: Tierdev\ndocs/Project.toml lists every weak dependency in [deps] on purpose. docs/make.jl loads them, then binds each extension module in Main through Base.get_extension and errors by name if one is missing, so a broken extension fails the build instead of quietly emptying the API reference. makedocs passes all six modules alongside Montage, with checkdocs=:all — every docstring in the core and in the extensions has to appear on some page, so API reference and the internal reference below together cover all of them.docs/make.jl includes docs/journal.jl, which regenerates docs/src/dev/journal.md from every !!! tierjournal block under docs/src, newest first, and warns when a page's tier blocks run out of order (they go brief, full, dev, journal within a section). The generated journal is committed, so it reads on GitHub too; edit the blocks on their own pages, never that file. Docstrings are rendered by docs/src/reference.md and by the @autodocs blocks below, so pages link to them with montage(ref) rather than declaring their own.","title":"Building the docs"},{"category":"section","location":"dev/architecture/#Adding-an-extension","page":"Architecture","text":"Step Where\nDeclare the function, type or hook in the core first. src/ — an extension can only add methods to a name the parent already owns.\nAdd the trigger package to [weakdeps] and an [extensions] entry naming every trigger. Project.toml, plus a [compat] bound for the new weakdep.\nName the file for the extension module, and make the module name match. ext/Montage<Trigger>Ext.jl, with the module declared inside.\nAdd methods only, qualified to the core (function Montage.montage(…)). The new ext/ file.\nMirror the new trigger in the docs environment. docs/Project.toml [deps] and [compat], plus the EXTENSIONS list in docs/make.jl.\n\n[weakdeps]\nNewDep = \"00000000-0000-0000-0000-000000000000\"\n\n[extensions]\nMontageNewDepExt = \"NewDep\"\n\ntierdev: Tierdev\nAn [extensions] value may be a list, and a multi-package extension loads only when every one of them is present — MontageCairoMakiePCMMExt = [\"CairoMakie\", \"PhysiCellModelManager\", \"PhysiCellOutput\"]. Because PhysiCellModelManager depends on PhysiCellOutput, loading PCMM alongside Montage activates both PhysiCell extensions at once.ext/MontagePhysiCellModelManagerExt.jl is the worked example of the shape to copy: 138 lines, one helper that maps an id to an output folder, and methods that wrap the result in a PhysiCellOutput handle and forward every other keyword untouched. Everything a PhysiCell reader needs is one layer down. When an adapter starts parsing output itself, the logic belongs in the extension it delegates to.If a helper has to be reachable from an extension that cannot using the one defining it, declare it in core — with no methods when nothing in core calls it, as _cellTypeLegend does, or with a strictly more general fallback when core needs a graceful error, as _recordSVGMovie does.","title":"Adding an extension"},{"category":"section","location":"dev/architecture/#Internal-reference","page":"Architecture","text":"The unexported helpers named above, from the core and from each extension.","title":"Internal reference"},{"category":"constant","location":"dev/architecture/#Montage._TITLE_FONT_SIZE","page":"Architecture","text":"Font size (px) of the bold panel titles drawn by _svgGrid.\n\nAlso the default size for a drawn legend's text, so legend and titles match. (A legend given as an SVG file is not rescaled to it — that one is placed at its natural size.) Lives here rather than in svg_backend.jl because MontageSpec's old-arity constructor defaults to it, and types.jl is included first.\n\n\n\n\n\n","title":"Montage._TITLE_FONT_SIZE"},{"category":"type","location":"dev/architecture/#Montage.MakieBackend","page":"Architecture","text":"CairoMakie backend — real heatmaps/colorbars, tableau, and movies. Its methods live in MontageCairoMakieExt and are available only after using CairoMakie.\n\n\n\n\n\n","title":"Montage.MakieBackend"},{"category":"type","location":"dev/architecture/#Montage.MontageBackend","page":"Architecture","text":"montageBackend(sym::Symbol) -> MontageBackend\n\nMap a user-facing backend keyword (:svg, :makie) to its selector type. Unknown symbols throw.\n\n\n\n\n\n","title":"Montage.MontageBackend"},{"category":"type","location":"dev/architecture/#Montage.SVGBackend","page":"Architecture","text":"SVG string-stitch backend — the core default. Lossless vector, static only.\n\n\n\n\n\n","title":"Montage.SVGBackend"},{"category":"method","location":"dev/architecture/#Montage._asPanels-Tuple{AbstractVector{Panel}}","page":"Architecture","text":"_asPanels(items) -> Vector{Panel}\n\nNormalize a loose collection into Vector{Panel}. Accepts a vector of Panels (returned as-is) or a vector of raw contents (each wrapped as an untitled Panel).\n\n\n\n\n\n","title":"Montage._asPanels"},{"category":"method","location":"dev/architecture/#Montage._assertWritable-Tuple{Any, Any}","page":"Architecture","text":"_assertWritable(path, overwrite)\n\nThrow if path exists and overwrite is false — the shared non-clobber guard for every core write (montage, record).\n\n\n\n\n\n","title":"Montage._assertWritable"},{"category":"function","location":"dev/architecture/#Montage._cellTypeLegend","page":"Architecture","text":"_cellTypeLegend(folders) -> Vector{Tuple{String,String}}\n\nCore-declared hook: the (label, colour) entries describing the cell types in one or more simulation output folders. Implemented by MontagePhysiCellOutputExt, which owns the PhysiCellOutput weakdep and knows the output-folder layout.\n\nDeclared here, rather than privately in that extension, so the CairoMakie extensions can reach it too — tableau uses it to colour cells with PhysiCell's own colours, so a tableau and a montage/storyboard of the same run agree. Extensions cannot using one another, so a core-owned hook is the supported way to share this.\n\nDeclared with no methods on purpose. Core never calls it, and every caller is an extension that necessarily loads MontagePhysiCellOutputExt alongside itself (activating a CairoMakie+PhysiCellOutput extension implies PhysiCellOutput is present). Giving it an untyped fallback here instead would make the extension's identically-signed method an overwrite rather than an addition, which Julia rejects during precompilation — the reason _recordSVGMovie and _montage take a deliberately more general fallback signature than their extension methods.\n\n\n\n\n\n","title":"Montage._cellTypeLegend"},{"category":"method","location":"dev/architecture/#Montage._escapeXML-Tuple{AbstractString}","page":"Architecture","text":"Escape the five XML special characters so titles can't break the SVG.\n\n\n\n\n\n","title":"Montage._escapeXML"},{"category":"method","location":"dev/architecture/#Montage._frameTransform-Tuple{AbstractVector, Integer}","page":"Architecture","text":"_frameTransform(transform, t) -> Function\n\nThe transform for frame t: a Vector of transforms is indexed, anything else (a plain function) is used as-is for every frame.\n\n\n\n\n\n","title":"Montage._frameTransform"},{"category":"method","location":"dev/architecture/#Montage._isAnimated-Tuple{Panel}","page":"Architecture","text":"_isAnimated(panel) -> Bool\n\nA panel is animated when its content is a sequence of per-timepoint frames (a Vector) rather than a single static image. Animated panels turn a montage into a MontageSpec that record can render as a movie.\n\n\n\n\n\n","title":"Montage._isAnimated"},{"category":"method","location":"dev/architecture/#Montage._legendLayout-Tuple{Any, Real}","page":"Architecture","text":"_legendLayout(entries, avail_w; font_size) -> (placed, w, h, r, gap)\n\nFlow-lay entries — (label, color) pairs — left to right at font_size, wrapping to a new row when the next entry would exceed avail_w. Returns the placed items (relative to the legend's own origin), the used size, the marker radius and the marker→label gap.\n\nBecause the legend is drawn rather than scaled from a source image, it is authored at the composition's title size and simply wraps to fit whatever space it is given. That is what makes the placement rules simple: there is no scale factor and no minimum-legibility problem.\n\nText width is estimated at 0.58 · font_size per character (about right for Arial); the result only affects wrapping and the reported width, so a small error is harmless.\n\n\n\n\n\n","title":"Montage._legendLayout"},{"category":"method","location":"dev/architecture/#Montage._montageSpec-Tuple{AbstractVector{Panel}}","page":"Architecture","text":"_montageSpec(panels; panel_width, title_height, pad) -> MontageSpec\n\nBuild a MontageSpec from animated panels (content is a Vector of frame paths). Frames are aligned by index and truncated to the shortest sequence; a warning is emitted if the panels have differing frame counts.\n\n\n\n\n\n","title":"Montage._montageSpec"},{"category":"method","location":"dev/architecture/#Montage._nestedSVG-Tuple{AbstractString, Vararg{Any, 4}}","page":"Architecture","text":"_nestedSVG(svg_text, x, y, w, h) -> String\n\nReturn a self-contained, positioned copy of svg_text as a nested <svg> element. The child is placed at (x, y) in a w × h box and scaled — via a viewBox set to its intrinsic size plus preserveAspectRatio=\"xMidYMid meet\" — to fit while keeping its aspect ratio. The viewBox is what makes the child scale instead of clip.\n\n\n\n\n\n","title":"Montage._nestedSVG"},{"category":"method","location":"dev/architecture/#Montage._normalizeLegend-Tuple{Any}","page":"Architecture","text":"_normalizeLegend(legend) -> legend_or_nothing\n\nNormalize the legend keyword, which says what to draw. Where it goes is the separate legend_position keyword — the two are orthogonal, so any content can take any placement.\n\nlegend result\nnothing / false nothing — no legend\na vector of (label, color) entries the entries, drawn (empty ⇒ no legend)\na function (x, y, avail_w) -> (fragment, w, h) called to draw it\na path or SVG string the source, nested as-is\n:auto nothing in the core — it means \"discover from the data source\", which only the PhysiCell extensions can do; they resolve it to entries before calling in\n\n\n\n\n\n","title":"Montage._normalizeLegend"},{"category":"method","location":"dev/architecture/#Montage._px-Tuple{Any}","page":"Architecture","text":"_px(x) -> Float64\n\nRound a coordinate for emission. Legend geometry is built from fractions of the font size (0.45 · font_size for a marker radius, and so on), so it picks up float noise that would otherwise land verbatim in the SVG. Applied only where that noise arises, so the no-legend path stays byte-identical.\n\n\n\n\n\n","title":"Montage._px"},{"category":"method","location":"dev/architecture/#Montage._slotRC-Tuple{Integer, Integer}","page":"Architecture","text":"_slotRC(slot, ncols) -> (row, col)\n\nRow-major 1-based grid slot → (row, col). Used to find the first free cell after the last panel, which is where an :auto legend goes when the layout has a spare.\n\n\n\n\n\n","title":"Montage._slotRC"},{"category":"method","location":"dev/architecture/#Montage._svgDimensions-Tuple{AbstractString}","page":"Architecture","text":"_svgDimensions(svg_text) -> (width, height)\n\nExtract the intrinsic (width, height) (as Float64) from an SVG's root <svg> tag. Parsed from the tag — never hardcoded — so any source size works.\n\n\n\n\n\n","title":"Montage._svgDimensions"},{"category":"method","location":"dev/architecture/#Montage._svgFrame-Tuple{MontageSpec, Integer}","page":"Architecture","text":"_svgFrame(spec::MontageSpec, t::Integer) -> String\n\nCompose the montage SVG for timepoint t (1-based) by taking each panel's t-th frame and stitching them with the same grid logic as the static montage. The spec's legend is drawn into every frame, so it stays put for the whole movie. Each panel's transform comes along too — indexed by frame when it is a Vector (see Panel).\n\n\n\n\n\n","title":"Montage._svgFrame"},{"category":"method","location":"dev/architecture/#Montage._svgGrid-Tuple{AbstractVector{Panel}}","page":"Architecture","text":"_svgGrid(panels; ncols, panel_width, title_height, pad,\n         legend_svg, legend_position, legend_font_size) -> String\n\nBuild a uniform titled grid of panels (row-major) and return the composed SVG as a string. ncols sets the number of columns — montage uses ceil(sqrt(n)) (the default), storyboard uses n (a single ordered row). Cells are uniform, sized from the largest intrinsic aspect ratio so nothing overflows or clips.\n\nThe title band is reserved for the whole grid only if at least one panel is titled; an all-untitled composition reserves no band (no wasted vertical space).\n\nlegend_svg (from _normalizeLegend) adds a legend, in one of three forms:\n\na vector of (label, color) entries, drawn at legend_font_size as flat circles and text (see _svgLegend);\na draw function (x, y, avail_w) -> (fragment, w, h), for a legend this module does not know how to build — the PhysiCell extension supplies a colorbar this way. Called once to measure and once to emit, so it must be pure;\na path or SVG string, nested and scaled to fit.\n\nPlaced per legend_position:\n\n:auto — the free cells trailing the last row, costing no space at all, else a full-width band below. A drawn legend wraps to whatever width it gets, so the run is always usable; a nested source SVG has to shrink, so it takes the run only if that is wide enough to stay legible.\n:bottom / :top — a full-width band at that edge;\n(row, col) or (row, col, span) — those explicit cells, growing the grid if the row is past the last one.\n\nWith legend_svg === nothing the output is byte-identical to a grid built without any legend support at all.\n\n\n\n\n\n","title":"Montage._svgGrid"},{"category":"method","location":"dev/architecture/#Montage._svgLegend-Tuple{Any, Real, Real, Real}","page":"Architecture","text":"_svgLegend(entries, x0, y0, avail_w; font_size) -> (fragment, w, h)\n\nDraw entries as a legend at (x0, y0), laid out by _legendLayout.\n\nEmits flat, top-level <circle> and <text> elements — deliberately not a nested <svg>. Nested SVGs are the thing PowerPoint and Illustrator handle worst, and being able to open the composition in those tools and nudge the legend is a large part of why the output is SVG.\n\n\n\n\n\n","title":"Montage._svgLegend"},{"category":"method","location":"dev/architecture/#Montage._svgSource-Tuple{AbstractString}","page":"Architecture","text":"_svgSource(src) -> String\n\nRead src as SVG text: a path to an SVG file, or already-SVG text (detected by a leading <). Used by the external legend escape hatch (legend=\"my_legend.svg\"), which nests the file as-is; the built-in legend is drawn from entries instead — see _legendLayout.\n\n\n\n\n\n","title":"Montage._svgSource"},{"category":"method","location":"dev/architecture/#Montage._writeSVG-Tuple{Any, Nothing, Any}","page":"Architecture","text":"_writeSVG(svg, output, overwrite) -> svg\n\nWrite svg to output (guarded by _assertWritable) and return it; if output is nothing, return svg without writing. Shared by the SVG-backend verbs.\n\n\n\n\n\n","title":"Montage._writeSVG"},{"category":"method","location":"dev/architecture/#Montage._cellTypeLegend-Tuple{Any}","page":"Architecture","text":"Montage._cellTypeLegend(folders) -> Vector{Tuple{String,String}}\n\nThe (label, colour) entries for a composition, from each folder's legend.svg, unioned across folders so a sweep that mixed configs still explains every cell type any panel can contain. Order follows the config (first folder's order, with later folders' extra types appended).\n\nEmpty when no folder has a legend.svg, which the core then treats as \"no legend\".\n\nThis implements the core-declared hook so the CairoMakie extensions can reach it too — tableau colours its cells from the same source, keeping a tableau and a montage of one run consistent.\n\n\n\n\n\n","title":"Montage._cellTypeLegend"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._cellAttributes-Tuple{Any}","page":"Architecture","text":"_cellAttributes(svg_path) -> (Dict(id => type), Dict(id => dead))\n\nEach cell's type and dead flag, read from the snapshot SVG itself — used to apply the same filter to the value range that the drawing will apply to the cells.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._cellAttributes"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._cellTransform-Tuple{Any, Bool}","page":"Architecture","text":"_cellTransform(cell_types, include_dead; recolor=nothing) -> transform\n\nA Panel transform (see Panel) that edits one snapshot's cell layer: keeps only the requested cells and, when recolor is given, repaints each kept cell from its data. Returns identity when there is nothing to do, so an untouched panel really is untouched.\n\nFiltering and recolouring share one pass because both walk the same cell groups. recolor is a (values, (lo, hi), colormap) tuple: values maps cell ID to the quantity, and (lo, hi) is fixed by the caller across every panel or frame so colours stay comparable.\n\nPhysiCell's own \"N agents\" label is rewritten to the number actually shown — a figure captioned 511 agents while displaying 200 of them would be wrong.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._cellTransform"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._cellValues-Tuple{Any, Any, Any}","page":"Architecture","text":"_cellValues(folder, index, column) -> Dict{Int,Float64}\n\nMap each cell's ID to its value of column, for the state the panel shows. Reads only the cells table (no substrates, no mesh).\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._cellValues"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._colorPlan-NTuple{5, Any}","page":"Architecture","text":"_colorPlan(color, panel_states, cell_types, include_dead, colormap) -> (transforms, colorbar)\n\nWork out how to paint each panel. panel_states[p] lists the (folder, index) states panel p shows — one for a still, many for a movie panel.\n\nReturns a Panel transform per panel (a plain function for a still, a per-frame Vector for a movie) and, when color is set, a colorbar draw function to use as the legend.\n\nThe value range is pooled over every state of every panel, so a colour means the same thing in each panel of a montage and each frame of a movie — ranging panels separately would make them mutually incomparable, which is the whole point of a montage. It is computed after filtering, so cells that will not be drawn cannot stretch the scale.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._colorPlan"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._keptLegend-Tuple{Any, Any}","page":"Architecture","text":"_keptLegend(entries, cell_types) -> entries\n\nNarrow the legend to the cell types actually kept, so a filtered figure's key matches what is on screen instead of advertising types that were removed. Applies only to a discovered (:auto) legend — an explicit one is the caller's own and is left alone.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._keptLegend"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._legendFor-NTuple{4, Any}","page":"Architecture","text":"_legendFor(legend, colorbar, folders, cell_types) -> legend\n\nPick the legend. When cells are recoloured by data, :auto becomes the colorbar — a cell-type key would describe colours the figure no longer uses. Otherwise :auto resolves to the cell types as before, and an explicit legend always wins.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._legendFor"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._legendRows-Tuple{AbstractString}","page":"Architecture","text":"_legendRows(legend_svg_text) -> Vector{Tuple{String,String}}\n\nThe (label, colour) pairs in one PhysiCell legend.svg, in the config's cell-type order.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._legendRows"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._legendSVG-Tuple{Any}","page":"Architecture","text":"The run's legend.svg, or nothing when it has none (older PhysiCell, some 3-D runs).\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._legendSVG"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._rampColor-Tuple{Symbol, Real}","page":"Architecture","text":"_rampColor(colormap, t) -> String\n\nColour at position t ∈ [0,1] along colormap, as an SVG rgb(r,g,b) string, by linear interpolation between the ramp's anchor points.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._rampColor"},{"category":"function","location":"dev/architecture/#MontagePhysiCellOutputExt._resolveAuto","page":"Architecture","text":"_resolveAuto(legend, folders) -> legend\n\nTurn legend=:auto into real (label, colour) entries from the runs' legend.svg. Anything else — explicit entries, a path, or nothing — passes straight through, so a caller can override the content while still using legend_position to place it.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._resolveAuto"},{"category":"method","location":"dev/architecture/#MontagePhysiCellOutputExt._svgColorbar-Tuple{Any, Real, Real, Symbol}","page":"Architecture","text":"_svgColorbar(label, lo, hi, colormap; font_size) -> draw function\n\nA horizontal colorbar as a draw function (x, y, avail_w) -> (fragment, w, h), the form the core legend machinery accepts for legends it cannot build itself. Replaces the cell-type key when cells are coloured by a continuous value, where per-type swatches would mean nothing.\n\nEmitted as one gradient-filled <rect> plus flat <text> — a single object to nudge in Illustrator rather than dozens of slices.\n\n\n\n\n\n","title":"MontagePhysiCellOutputExt._svgColorbar"},{"category":"method","location":"dev/architecture/#MontageCairoMakiePhysiCellOutputExt._cellMask-Tuple{Any, Any, Bool, Any}","page":"Architecture","text":"_cellMask(cells, cell_types, include_dead, snap) -> BitVector\n\nWhich cells to plot. cell_types=nothing keeps every type; otherwise names are validated against the config's full type list, so selecting a type that happens to be absent from this snapshot is not an error. include_dead=false drops cells flagged dead.\n\n\n\n\n\n","title":"MontageCairoMakiePhysiCellOutputExt._cellMask"},{"category":"method","location":"dev/architecture/#MontageCairoMakiePhysiCellOutputExt._colorColumn-Tuple{Any, Any}","page":"Architecture","text":"_colorColumn(cells, color) -> Symbol\n\nResolve and validate the color keyword against the cells table. The table has ~130 columns, so the error points at the function that lists them rather than dumping them.\n\n\n\n\n\n","title":"MontageCairoMakiePhysiCellOutputExt._colorColumn"},{"category":"method","location":"dev/architecture/#MontageCairoMakiePhysiCellOutputExt._configuredTypes-Tuple{Any}","page":"Architecture","text":"Cell types the model defines, per the snapshot's config — not just those currently alive.\n\nOrdered as the config orders them, taken from the run's legend.svg when it has one, so a tableau's legend lists types in the same sequence a montage/storyboard legend does. Falls back to the snapshot XML (sorted, since the dict carries no order) when there is no legend.svg.\n\n\n\n\n\n","title":"MontageCairoMakiePhysiCellOutputExt._configuredTypes"},{"category":"method","location":"dev/architecture/#MontageCairoMakiePhysiCellOutputExt._paletteOrder-Tuple{Any, Symbol, Any}","page":"Architecture","text":"_paletteOrder(cells, col, snap) -> Vector{String}\n\nThe value order that fixes which palette slot each category gets. Taken from the unfiltered column — and, when colouring by cell type, unioned with the config's full type list — so that filtering to a subset, or a type dying out, never reshuffles colours. Without this, caf would be the third series (and third colour) in a full plot but the first in cell_types=[\"caf\",\"nk\"], making the two figures impossible to compare.\n\n\n\n\n\n","title":"MontageCairoMakiePhysiCellOutputExt._paletteOrder"},{"category":"method","location":"dev/architecture/#MontageCairoMakiePhysiCellOutputExt._physiCellColors-Tuple{Any}","page":"Architecture","text":"_physiCellColors(folder) -> Dict{String,String}\n\nEach cell type's own PhysiCell colour, from the run's legend.svg via the core-declared Montage._cellTypeLegend hook. Using these means a tableau and a montage/storyboard of the same run agree, instead of the tableau inventing Makie-palette colours for types the user already recognises by colour. Empty when the run has no legend.svg, in which case palette slots are used.\n\n\n\n\n\n","title":"MontageCairoMakiePhysiCellOutputExt._physiCellColors"},{"category":"method","location":"dev/architecture/#MontageCairoMakiePhysiCellOutputExt._seriesColor-Tuple{Any, Any, Any}","page":"Architecture","text":"_seriesColor(pc, order, v)\n\nColour for the series of category v: PhysiCell's own colour when known, else a palette slot held stable by order (never by plotting order — see _paletteOrder).\n\n\n\n\n\n","title":"MontageCairoMakiePhysiCellOutputExt._seriesColor"},{"category":"section","location":"man/extensions/#extensions-page","page":"Extensions","text":"Montage's core has no dependencies at all — its Project.toml carries no [deps] section — and everything that needs a heavy stack arrives as a package extension, which Julia loads for you the moment its trigger packages are in the session.","title":"Extensions"},{"category":"section","location":"man/extensions/#The-six-extensions","page":"Extensions","text":"tierbrief: Tierbrief\nThey fall into three groups: one extension renders montage movies, two are doors onto PhysiCell output (by folder, or by simulation id), and three build on CairoMakie to provide tableau. Each one only adds methods to names the core already owns — montage, storyboard, tableau, and the renderer hook behind record.\n\ntierfull: Tierfull\nThe two PhysiCell doors are deliberately lopsided. MontagePhysiCellOutputExt holds the reading logic; MontagePhysiCellModelManagerExt is a thin adapter that resolves a simulation id to its output folder and hands off. The CairoMakie trio splits the same way: the layout engine, then the PhysiCell plotting, then the id adapter.What none of them adds is a Makie backend for montage or storyboard. Those two verbs stitch SVG, which is lossless, editable and free of heavy dependencies, so they stay in the core whatever you load; backend=:makie errors. Tableau is the one verb that re-plots from data, which is why it is the one verb CairoMakie is required for.\n\nExtension Trigger packages What it adds\nMontageMovieExt Rsvg, Cairo, FFMPEG the renderer behind record: each stitched SVG frame rasterized, then encoded with FFMPEG\nMontagePhysiCellOutputExt PhysiCellOutput montage and storyboard over PhysiCell output folders — PhysiCellSequence and PhysiCellSnapshot\nMontagePhysiCellModelManagerExt PhysiCellModelManager, PhysiCellOutput those same two verbs by simulation id: montage(Simulation, ids)\nMontageCairoMakieExt CairoMakie tableau in its data-agnostic form, tableau(focal, satellites)\nMontageCairoMakiePhysiCellOutputExt CairoMakie, PhysiCellOutput tableau of a PhysiCell output folder, still or movie\nMontageCairoMakiePCMMExt CairoMakie, PhysiCellModelManager, PhysiCellOutput tableau by simulation id","title":"The six extensions"},{"category":"section","location":"man/extensions/#One-using-line-is-usually-enough","page":"Extensions","text":"tierbrief: Tierbrief\nAn extension activates once all of its trigger packages are present, so you name only the package you actually work with. using PhysiCellModelManager covers both PhysiCell extensions; using PhysiCellOutput is the door for people driving Montage straight from output folders.\n\ntierfull: Tierfull\nA Julia extension triggers when its trigger packages are loaded for any reason, including as another package's dependency rather than by name in your script. PhysiCellModelManager depends on PhysiCellOutput, so using PhysiCellModelManager puts both in the session and satisfies the [\"PhysiCellModelManager\", \"PhysiCellOutput\"] trigger without you typing the second name. The trigger list names both because the extension's code uses both: it calls PCMM to resolve an id to a folder, then constructs a PhysiCellOutput.PhysiCellSequence over it. Montage's compat bound is PhysiCellModelManager = \"0.4, 0.5\", so this holds for every version Montage supports.CairoMakie is orthogonal to all of that. On its own it adds the generic tableau; combined with either PhysiCell door it also adds the simulation methods, because the fifth and sixth rows of the table above are separate extensions with their own triggers.\n\nusing Montage, PhysiCellModelManager              # montage/storyboard by simulation id\nusing Montage, PhysiCellOutput                    # ...from output folders, without PCMM\nusing Montage, CairoMakie                         # tableau(focal, satellites)\nusing Montage, CairoMakie, PhysiCellModelManager  # tableau(Simulation, id)\nusing Montage, Rsvg, Cairo, FFMPEG                # montage movies\n\ntierjournal: 2026-09-21 — PCMM 0.4+ brings PhysiCellOutput along\nThe PhysiCellModelManager extension is triggered by PCMM and PhysiCellOutput together, because it uses both. PhysiCellModelManager 0.4 took PhysiCellOutput as a dependency, so loading PCMM now loads PhysiCellOutput too and the trigger is satisfied without the user naming it — verified on 0.4.0 and 0.5.1. The compat bound rose to \"0.4, 0.5\" to make that promise true rather than conditional: under 0.3 the extension needed a using PhysiCellOutput the reader had no way to guess.","title":"One using line is usually enough"},{"category":"section","location":"man/extensions/#When-something-is-missing","page":"Extensions","text":"tierbrief: Tierbrief\nA verb whose extension is absent errors with the packages to load written into the message, so the fix is the error text.\n\ntierfull: Tierfull\nThe PhysiCell methods are the exception, because there is nothing for Montage to catch. Without PhysiCellModelManager the name Simulation does not exist, so the call fails before Montage sees it; without PhysiCellOutput you cannot build a PhysiCellSequence to pass either. A folder path handed to montage instead falls through to the SVG backend, which wants one SVG file per panel — see PhysiCell simulations for what to pass.\n\ntableau(Simulation, 1)                        # PCMM loaded, CairoMakie not\n# ERROR: `tableau` needs the CairoMakie extension (and PhysiCellOutput or PhysiCellModelManager\n#        for simulations) — run `using CairoMakie, PhysiCellOutput` or\n#        `using CairoMakie, PhysiCellModelManager` first to get it.\n\nmontage([Panel([\"a/f1.svg\", \"a/f2.svg\"])])    # no Rsvg/Cairo/FFMPEG loaded\n# ERROR: montage movies need the movie extension — run `using Rsvg, Cairo, FFMPEG` to load it\n\nmontage(Simulation, [1, 2, 3])                # UndefVarError: `Simulation` is PCMM's name\n\nmontage([\"run1/output\"])                      # a folder is not an SVG file\n# ERROR: SVG file not found: run1/output","title":"When something is missing"},{"category":"section","location":"man/extensions/#Why-extensions-rather-than-separate-packages","page":"Extensions","text":"tierbrief: Tierbrief\nExtensions decouple what you type from where the code lives. using Montage is the only Montage import there ever is, and loading CairoMakie or a PhysiCell package widens what the same three verbs accept.\n\ntierfull: Tierfull\nThe alternative worth weighing is a companion package — a PhysiCell-flavoured Montage you would install and import alongside this one. It costs a second name to learn, and its verbs could not be montage and storyboard unless it depended on Montage and added methods to them, which is exactly what an extension does with none of the packaging. So: the core carries no heavy dependency, montage stays one function whose meaning never shifts, and methods(montage) simply grows as you load more.The constraint that makes this work is that an extension may only add methods to functions the core already owns. That is why the verbs, the Panel and MontageSpec types, and the backend selectors are all declared in the core: an extension cannot introduce a new entry point that a user who typed only using Montage would be able to reach.\n\ntierjournal: 2026-07-27 — two front doors, one reader path\nPhysiCell support ships as Montage extensions rather than a separate PhysiCellMontage.jl package. The clunky name was not worth it, and extensions already decouple what a user types from where code lives — everyone still types using Montage. Folder paths go through MontagePhysiCellOutputExt, which holds the real logic; simulation ids go through MontagePhysiCellModelManagerExt, a thin adapter that resolves an id to a folder and delegates. Both read through PhysiCellOutput, so there is one reader path rather than two implementations that could drift.","title":"Why extensions rather than separate packages"},{"category":"section","location":"man/extensions/#How-an-extension-is-wired","page":"Extensions","text":"[weakdeps]\nCairoMakie = \"13f3f980-e62b-5c42-98c6-ff1f3baf88f0\"\nPhysiCellOutput = \"61ac5edc-a73d-4441-b68d-d9b510422d04\"\n\n[extensions]\nMontageCairoMakieExt = \"CairoMakie\"\nMontageCairoMakiePhysiCellOutputExt = [\"CairoMakie\", \"PhysiCellOutput\"]\n\ntierdev: Tierdev\nTrigger packages go in [weakdeps], never [deps] — a [deps] entry would make the dependency compulsory and defeat the whole arrangement. Each [extensions] key is the module name and must match a file under ext/ (MontageCairoMakieExt ↔ ext/MontageCairoMakieExt.jl), and its value lists every trigger package the module's code touches, not just the headline one. Give each weakdep a [compat] bound as well; that is the only place a version constraint on it can live.Inside ext/, a method that fills a core hook is written qualified — function Montage.tableau(…), function Montage._recordSVGMovie(…). A hook the core cannot implement at all is declared there as an empty generic function (function _cellTypeLegend end, in src/svg_backend.jl) purely so the extension has something to extend. A hook the core can fall back on is written on abstract or untyped arguments — _montage(::MontageBackend, …), _recordSVGMovie(spec, path, framerate, scale) — so the extension's concrete method is strictly more specific and no method-overwrite warning fires either way.docs/make.jl loads all six weakdeps and errors if Base.get_extension returns nothing for any of them, so a mis-wired trigger list fails the docs build instead of quietly emptying the API reference.","title":"How an extension is wired"},{"category":"section","location":"man/physicell/#physicell-page","page":"PhysiCell simulations","text":"All three verbs drive straight from PhysiCell output: a grid comparing runs, a filmstrip of one run over time, and a multi-channel scene of one state — the grid and the scene still or animated.","title":"PhysiCell simulations"},{"category":"section","location":"man/physicell/#Two-front-doors","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\nSimulation ids go through PhysiCellModelManager (PCMM). Output folder paths go through PhysiCellOutput, for work that does not use PCMM. The verbs and the keywords are the same on both sides; the panel titles and the default output path are what differ.\n\ntierfull: Tierfull\nBoth doors read through PhysiCellOutput, so there is one reader path rather than two: the PCMM methods resolve a simulation id to its output folder, wrap it in a PhysiCellSequence, and delegate to the folder methods. A PCMM user therefore types one using line — PhysiCellModelManager depends on PhysiCellOutput, so loading PCMM activates both PhysiCell extensions, and no third using is involved. Name PhysiCellOutput yourself only when you are driving Montage from output folders without PCMM.\n\nusing PhysiCellModelManager, Montage    # ids:     montage(Simulation, [1, 2, 3])\nusing PhysiCellOutput, Montage          # folders: montage(PhysiCellSequence(\"run01/output\"))\n\ntierdev: Tierdev\next/MontagePhysiCellOutputExt.jl holds the PhysiCell logic — snapshot selection, cell filtering, the legend, the colour plan. ext/MontagePhysiCellModelManagerExt.jl knows exactly one PCMM-specific thing, joinpath(trialFolder(Simulation, id), \"output\"), and forwards everything else. The CairoMakie pair for tableau splits the same way. See Extensions.","title":"Two front doors"},{"category":"section","location":"man/physicell/#What-is-read","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\nmontage and storyboard stitch PhysiCell's own snapshot renders — output/initial.svg, output/final.svg, output/snapshot00000000.svg and the rest — so a panel carries PhysiCell's styling exactly and neither verb needs a plotting stack.\n\ntierfull: Tierfull\ntableau is the exception. It sets the cells against each substrate field, which calls for real heatmaps and real colorbars, so it re-plots from the snapshot's cells, substrates and mesh instead of nesting a finished SVG. That is the one place a PhysiCell figure needs CairoMakie.\n\nVerb Reads Load\nmontage output/*.svg PhysiCellOutput, plus Rsvg, Cairo, FFMPEG for a movie\nstoryboard output/*.svg PhysiCellOutput\ntableau the snapshot's cells, substrates and mesh CairoMakie and PhysiCellOutput","title":"What is read"},{"category":"section","location":"man/physicell/#Comparing-simulations-—-montage","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\nName the simulations you want: montage(Simulation, simulationIDs()) is how you say \"all of them\". Each panel is one simulation, titled Sim <id> unless you pass your own title function of the id.\n\ntierfull: Tierfull\nindex decides what the panels show and, with it, whether the result is a still or a movie. A still skips any simulation missing that snapshot, with a warning, and errors when none of them have it. A movie plays every panel's snapshot series in lockstep, aligned by index and truncated to the shortest, and is rendered through record — see Movies.\n\nindex Result\n:final (the default), :initial, or an Integer snapshot index a still SVG\n:all, or a Vector/range of snapshot indices a movie\n\nusing PhysiCellModelManager, Montage\n\nmontage(Simulation, simulationIDs())                        # final state of every simulation\nmontage(Simulation, [1, 2, 3]; index=:initial)              # initial states of three runs\nmontage(Simulation, [1, 2, 3]; index=10, output=nothing)    # 10th snapshot, as an SVG string\nmontage(Simulation, 7)                                      # a single-panel montage\nmontage(Simulation, simulationIDs(); title=id -> \"run $id\", ncols=4)\n\ntierfull: Tierfull\nThe objects PCMM already hands you are accepted in place of ids, and resolve to their constituent simulations: a trial (Simulation, Monad, Sampling, Trial), a run output (PCMMOutput), or a vector of either.\n\nmontage(sampling)                             # every simulation in a trial\nmontage([monad1, monad2]; index=:all)         # two monads, animated\nmontage(out)                                  # out = run(sampling)\n\nusing Rsvg, Cairo, FFMPEG                     # the movie renderer\nmontage(Simulation, [1, 2, 22, 32]; index=:all, output=\"compare.mp4\", framerate=15)\n\ntierfull: Tierfull\nThe folder door takes a PhysiCellSequence per panel, or a vector of them, and titles each panel with the run folder's name — the directory holding output. A vector of PhysiCellSnapshots works too, when the states you want to compare are not the same snapshot in every run. Everything else — index, legend, cell_types, color, and the core grid keywords such as ncols and panel_width — is identical to the id door.\n\nusing PhysiCellOutput, Montage\n\nseqs = [PhysiCellSequence(joinpath(run, \"output\")) for run in [\"run01\", \"run02\", \"run03\"]]\nmontage(seqs)                                               # final states, titled run01, run02, …\nmontage(seqs; index=:all, output=\"compare.mp4\")             # the same grid, animated\n\nsnaps = [PhysiCellSnapshot(\"run01/output\", :final), PhysiCellSnapshot(\"run02/output\", 60)]\nmontage(snaps)                                              # states chosen run by run","title":"Comparing simulations — montage"},{"category":"section","location":"man/physicell/#One-simulation-over-time-—-storyboard","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\nstoryboard takes one simulation and lays its timepoints out in order, each frame titled with its simulation time. It is always static: the filmstrip is the point.\n\ntierfull: Tierfull\nChoose the timepoints one of two ways, not both. index is a vector of selectors, each an Integer snapshot index or :initial/:final, and gives you exact control. n_snapshots (default 4) takes that many evenly spaced snapshots spanning the run, endpoints included. Passing both with different lengths errors. title is a function of the frame's simulation time, so the units are yours to choose; ncols wraps a long strip into a block.\n\nstoryboard(Simulation, 1)                                   # 4 evenly spaced frames\nstoryboard(Simulation, 1; n_snapshots=6)\nstoryboard(Simulation, 1; index=[:initial, 30, 60, :final])\nstoryboard(Simulation, 1; title = t -> \"$(round(t / 1440; digits=1)) d\")\nstoryboard(Simulation, 1; n_snapshots=8, ncols=4, output=\"strip.svg\", overwrite=true)\n\ntierfull: Tierfull\nThrough the folder door, pass the sequence itself. The keywords are the same.\n\nstoryboard(PhysiCellSequence(\"run01/output\"); n_snapshots=5)","title":"One simulation over time — storyboard"},{"category":"section","location":"man/physicell/#A-multi-channel-scene-—-tableau","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\ntableau puts one simulation state in the middle — the cells, re-plotted as a scatter — and rings it with one heatmap and colorbar per substrate. Every panel shares the mesh's spatial extent, so a feature in the cells sits over the same coordinates in every field. It needs CairoMakie as well.\n\ntierfull: Tierfull\nindex works exactly as it does for montage: a single selector gives a still, :all or a vector of indices animates the whole scene through Makie.record, with each substrate's colorrange fixed to its global min/max so the fields stay comparable frame to frame. substrates chooses which fields get satellites (all of them by default), colormap is the substrate ramp, markersize the cell size, and size the figure size.\n\nusing CairoMakie, PhysiCellModelManager, Montage\n\ntableau(Simulation, 1)                                          # cells plus every substrate\ntableau(Simulation, 1; index=60, substrates=[\"oxygen\"], output=\"oxygen.pdf\")\ntableau(Simulation, 1; index=:all, framerate=15)                # writes tableau.mp4\ntableau(Simulation, 1; index=:final, size=(1400, 1400), output=nothing)   # returns the Figure\n\ntierfull: Tierfull\nA still accepts output=nothing and returns the Figure for further Makie work; a movie has to be written to a file. .png is the default, .svg and .pdf also work — see Tableau for which to choose.\n\nusing CairoMakie, PhysiCellOutput, Montage\n\ntableau(PhysiCellSnapshot(\"run01/output\", :final))\ntableau(PhysiCellSequence(\"run01/output\"); index=0:5:120, output=\"scene.mp4\")","title":"A multi-channel scene — tableau"},{"category":"section","location":"man/physicell/#Choosing-which-cells-appear","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\ncell_types (one name or a vector of names) and include_dead=false restrict which cells are drawn. Both work on all three verbs, with the same meaning.\n\ntierfull: Tierfull\nFor the stitched verbs this is a pure edit of the snapshot SVG: PhysiCell tags every cell as <g id=\"cell442\" type=\"tumor_epi\" dead=\"true\">, so the non-matching groups are dropped and nothing is re-rendered. PhysiCell's own \"N agents\" caption is rewritten to the number actually shown, since a figure captioned 511 agents while displaying 200 of them would be wrong, and an automatic legend narrows to the types you kept. tableau applies the same selection to the data before plotting, and validates the names against the config's full type list, so filtering to a type that happens to be absent from one snapshot gives an empty panel rather than an error.\n\nmontage(Simulation, simulationIDs(); cell_types=\"tumor_epi\")\nstoryboard(Simulation, 1; cell_types=[\"tumor_epi\", \"caf\"], include_dead=false)\ntableau(Simulation, 1; cell_types=\"caf\", include_dead=false)","title":"Choosing which cells appear"},{"category":"section","location":"man/physicell/#The-cell-type-legend","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\nThe stitched verbs include a cell-type key by default (legend=:auto), in PhysiCell's own colours. legend=nothing suppresses it; legend_position places it.\n\ntierfull: Tierfull\nThe entries come from each run's output/legend.svg, which lists every cell type the config defines together with the colour PhysiCell drew it in, and they are unioned across panels so a sweep that mixed configs still explains every type any panel can contain. Because the key describes what the model can contain rather than what one snapshot happens to show, one legend is correct for every frame of a movie, and it is drawn into each of them. The entries are drawn as flat circles and text, so they stay editable in Illustrator and PowerPoint. legend_position is independent of the content: :auto uses the free cells trailing the last row when the grid has any, costing no space, and otherwise a band below.\n\nmontage(Simulation, [1, 2]; legend=nothing)                    # no key\nmontage(Simulation, [1, 2]; legend_position=:bottom)           # a band below, not the spare cells\nmontage(Simulation, [1, 2]; legend=[(\"treated\", \"red\"), (\"control\", \"grey\")])\nmontage(Simulation, [1, 2]; legend=\"key.svg\")                  # nest a hand-made file\n\ntierdev: Tierdev\nThe parse lives in Montage._cellTypeLegend(folders), a hook declared in the core so that tableau can reach it too: the tableau draws its cells in the same PhysiCell colours, which is what keeps a tableau and a montage of one run agreeing. A run with no legend.svg — older PhysiCell, some 3-D runs — yields no entries, and :auto then means no legend.\n\ntierjournal: 2026-08-05 — the legend comes from PhysiCell's own legend.svg\nThe run already writes one file listing every cell type the config defines, with the colours PhysiCell drew them in. Reading it costs one ~1.5 KB file per simulation and is independent of frame count, and because it describes what the model can contain rather than what one snapshot happens to show, a single legend is correct for every frame of a movie. Rejected scanning snapshots for the types actually present: it is per-frame work, and it produces a legend that changes as cells appear and die. The entries are drawn as flat circles and text rather than nested as an SVG so they stay editable in Illustrator and PowerPoint.","title":"The cell-type legend"},{"category":"section","location":"man/physicell/#Colouring-cells-by-data","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\ncolor names any column of the cells table — cellLabels(snapshot) lists them — and repaints each cell along colormap. For the stitched verbs the cell-type key becomes a colorbar, since per-type swatches would describe colours the figure does not use.\n\ntierfull: Tierfull\nEach cell group in the snapshot SVG carries its id=\"cell442\", which joins to the ID column of the cells table, so a column can drive the fill without re-rendering anything. The value range is pooled over every panel and every frame and computed after filtering: a colour then means the same quantity everywhere in the figure, which is the whole point of a montage, and cells you excluded cannot stretch the scale. The stitched path carries a small built-in colormap set — :viridis, :plasma, :grays — and takes a numeric column only; a categorical column belongs in tableau, which draws one labelled series per value.\n\nusing PhysiCellOutput\ncellLabels(PhysiCellSnapshot(\"run01/output\", :final))   # the columns available to `color`\n\nmontage(Simulation, [1, 2, 3]; color=:pressure, colormap=:plasma)\nstoryboard(Simulation, 1; n_snapshots=5, color=:total_volume)\n\ntierfull: Tierfull\ntableau re-plots, so it has Makie's full colormap set and both visual modes. color picks the column — the cells' own columns, plus :cell_type_name — cell_colormap is the cell ramp (colormap stays the substrates'), and color_mode overrides the automatic choice between the two modes.\n\ncolor column color_mode=:auto draws Key\n:cell_type_name (the default), or any string or Bool column one labelled scatter series per value a legend\nany other numeric column, such as :pressure one scatter on cell_colormap a colorbar\n\ntableau(Simulation, 1; color=:pressure, cell_colormap=:plasma)\ntableau(Simulation, 1; color=:current_phase, color_mode=:categorical)\ntableau(Simulation, 1; index=:all, color=:pressure, output=\"pressure.mp4\")\n\ntierfull: Tierfull\nA numeric column whose values are really labels — :current_phase is the usual one — reads as continuous unless you say color_mode=:categorical. In a movie a continuous color gets a globally fixed colorrange, its min/max over every frame after filtering. That matters more than it sounds: at t = 0 a quantity like pressure is often uniformly zero, and a per-frame scale would spread that single value across the whole ramp and show a meaningless riot of colour.\n\ntierjournal: 2026-08-05 — a built-in colormap set on the stitched path\nColouring cells by a data value rewrites fills in the snapshot SVG, so it needs a colormap but not a plotting stack. Carrying :viridis, :plasma and :grays in the core keeps montage and storyboard dependency-free; tableau already re-plots through Makie and has the full set.","title":"Colouring cells by data"},{"category":"section","location":"man/physicell/#Where-the-files-go","page":"PhysiCell simulations","text":"tierbrief: Tierbrief\nEvery verb writes by default: the id door under dataDir()/outputs, where the rest of a PCMM project's output lives, and the folder door into the current directory. A stitched verb hands the composed SVG back as well; a written tableau or movie hands back the path.\n\ntierfull: Tierfull\nWriting errors if the file already exists, unless you pass overwrite=true — a guard worth having when a figure is regenerated from a script. output=nothing skips writing and hands back the in-memory result: the composed SVG string for a still montage or storyboard, a MontageSpec for an animated montage, a Figure for a still tableau. A movie must be written to a path.\n\nCall Default output\nmontage(Simulation, …) dataDir()/outputs/montage.svg, or montage.mp4 for a movie\nstoryboard(Simulation, …) dataDir()/outputs/storyboard.svg\ntableau(Simulation, …) dataDir()/outputs/tableau.png, or tableau.mp4 for a movie\nmontage(seqs), storyboard(seq) montage.svg / storyboard.svg, in the current directory\ntableau(snap), tableau(seq) tableau.png, or tableau.mp4 for a movie","title":"Where the files go"},{"category":"section","location":"#Montage.jl","page":"Home","text":"Montage composes visualizations — PhysiCell's own renders, or any SVGs you can point it at — into deliberately structured composite figures and movies.","title":"Montage.jl"},{"category":"section","location":"#The-three-verbs","page":"Home","text":"Verb Use it to… Looks like\nmontage compare like-for-like across many things a uniform grid of equal panels\nstoryboard show one thing evolving over time an ordered, static filmstrip\ntableau show how heterogeneous parts of one state relate spatially a focal panel ringed by satellites\n\ntierbrief: Tierbrief\nA real montage is the final state of every simulation in a batch, side by side in one grid. A real storyboard is one simulation's time course, four frames left to right, each titled with its timestamp. A real tableau is one simulation at one moment: the cell layer centred, each substrate's heatmap and colorbar ringed around it, every axis on the same spatial extent.Still or animated is a separate choice from which verb you reach for. montage and tableau each render a still figure or a movie; storyboard is static by design, because it is the filmstrip.\n\ntierfull: Tierfull\nOne general compose(panels; layout) could draw all three shapes, so three verbs instead of one layout argument is a deliberate cost. The shape is not the point — the claim is. A grid of equal cells says these are comparable; a left-to-right strip says read me in order; a centred panel with satellites says these all describe one state. Naming the claim makes it the thing you choose first, and it lets each verb take the defaults that claim implies: montage grids near-square, storyboard stays on one row and puts timestamps in its titles, tableau shares one spatial extent across every axis.The two movie paths are built differently — a montage movie stitches one SVG per timepoint and hands the rasterized frames to FFMPEG, while a tableau movie is recorded by Makie — but the choice reads the same at the call site. Panels carrying a single image each give a still; panels carrying frame sequences give a movie. See Movies.","title":"The three verbs"},{"category":"section","location":"#Installation","page":"Home","text":"tierbrief: Tierbrief\nThe registry also carries PhysiCellModelManager.jl and PhysiCellOutput.jl, so you add it once and can then install any of the three.\n\npkg> registry add https://github.com/drbergman-lab/BergmanLabRegistry\n\npkg> add Montage","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"tierbrief: Tierbrief\nA Panel is one cell of a composition: some content, plus an optional title drawn above it. Every verb writes its result by default — pass output=nothing to skip the write and just take the result, and overwrite=true to write over a file that is already there.\n\ntierfull: Tierfull\nCells are uniform and sized from the largest aspect ratio among the panels, so nothing clips, and each panel's intrinsic size is parsed out of its own SVG rather than assumed. A title band is reserved for the whole grid only when at least one panel is titled, so an untitled composition wastes no vertical space. What comes back is a real SVG document whose titles and legends are live <text>, which keeps the figure editable in Illustrator or PowerPoint.\n\nsvg = montage([Panel(a; title=\"A\"), Panel(b; title=\"B\")]; output=nothing)\nMain.SVGFigure(\"<?xml version=\\\"1.0\\\" encoding=\\\"UTF-8\\\" standalone=\\\"no\\\"?>\\n<svg xmlns=\\\"http://www.w3.org/2000/svg\\\" width=\\\"636.0\\\" height=\\\"358.0\\\" viewBox=\\\"0 0 636.0 358.0\\\">\\n<rect x=\\\"0\\\" y=\\\"0\\\" width=\\\"636.0\\\" height=\\\"358.0\\\" fill=\\\"white\\\"/>\\n<text x=\\\"162.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">A</text>\\n<svg x=\\\"12.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"tomato\\\"/>\\n</svg>\\n<text x=\\\"474.0\\\" y=\\\"36.0\\\" text-anchor=\\\"middle\\\" font-family=\\\"Arial\\\" font-size=\\\"22\\\" font-weight=\\\"bold\\\" fill=\\\"black\\\">B</text>\\n<svg x=\\\"324.0\\\" y=\\\"46.0\\\" width=\\\"300\\\" height=\\\"300.0\\\" viewBox=\\\"0 0 120.0 120.0\\\" preserveAspectRatio=\\\"xMidYMid meet\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n<circle cx=\\\"60\\\" cy=\\\"60\\\" r=\\\"50\\\" fill=\\\"steelblue\\\"/>\\n</svg>\\n</svg>\\n\")\n\nstoryboard([Panel(\"t0.svg\"; title=\"t = 0\"),   Panel(\"t1.svg\"; title=\"t = 120\"),\n            Panel(\"t2.svg\"; title=\"t = 240\"), Panel(\"t3.svg\"; title=\"t = 360\")];\n           output=\"timecourse.svg\")","title":"Quick start"},{"category":"section","location":"#A-light-core,-extensions-for-the-rest","page":"Home","text":"tierbrief: Tierbrief\nThe core does the stitching and nothing else: it reads SVG documents as text, nests them in a grid, and writes one SVG back out, with no plotting stack underneath. Everything heavier arrives as a package extension, which Julia activates the moment the packages it names are loaded. Movies come with Rsvg, Cairo and FFMPEG, which rasterize each stitched frame and encode it. tableau comes with CairoMakie, which it needs for real axes, heatmaps and colorbars — it re-plots from data rather than stitching renders. PhysiCell support comes with PhysiCellOutput, for output folders, and PhysiCellModelManager, for simulation ids. Extensions has the full table.\n\nusing Montage                  # montage and storyboard, stitching SVGs\nusing Rsvg, Cairo, FFMPEG      # montage movies\nusing CairoMakie               # tableau, in its data-agnostic form\nusing PhysiCellModelManager    # drive any verb from simulation ids","title":"A light core, extensions for the rest"},{"category":"section","location":"#PhysiCell-simulations","page":"Home","text":"tierbrief: Tierbrief\nMontage grew up in the PhysiCell ecosystem, and the verbs know how to read a run. using PhysiCellModelManager, Montage is all it takes to drive them from simulation ids: PhysiCellModelManager depends on PhysiCellOutput, so both PhysiCell extensions activate together and ids resolve to output folders for you. Working straight from output folders instead, using PhysiCellOutput gives the same verbs on a PhysiCellSequence or a PhysiCellSnapshot, with no PhysiCellModelManager in sight. PhysiCell simulations covers cell-type legends, filtering, colouring cells by a data column, and the index rule that decides still versus movie.\n\nusing PhysiCellModelManager, Montage\n\nmontage(Simulation, simulationIDs())    # every simulation's final state, in one grid\nstoryboard(Simulation, 32)              # simulation 32's time course, four frames","title":"PhysiCell simulations"},{"category":"section","location":"#Manual","page":"Home","text":"Pages = [\"man/montage.md\", \"man/storyboard.md\", \"man/tableau.md\",\n         \"man/movies.md\", \"man/physicell.md\", \"man/extensions.md\"]\nDepth = 1","title":"Manual"}]
}
