var documenterSearchIndex = {"docs":
[{"category":"section","location":"man/montage/#Montage","page":"Montage","text":"montage arranges a collection of homogeneous panels into a uniform titled grid — the verb for comparing like-for-like across many things (e.g. the final state of every run in a batch, side by side).","title":"Montage"},{"category":"section","location":"man/montage/#Panels","page":"Montage","text":"A composition is a vector of Panels. A panel carries its content and an optional title:\n\nusing Montage\n\npanels = [Panel(\"a/final.svg\"; title=\"A\"),\n          Panel(\"b/final.svg\"; title=\"B\"),\n          Panel(\"c/final.svg\"; title=\"C\")]\nmontage(panels)\n\nFor the default :svg backend, a panel's content is a path to an SVG file — each is inlined and scaled to fit its cell (losslessly, as vector graphics). You can also pass a loose vector of raw contents and they are wrapped as untitled panels:\n\nmontage([\"a/final.svg\", \"b/final.svg\"])   # untitled","title":"Panels"},{"category":"section","location":"man/montage/#The-grid","page":"Montage","text":"Panels are laid out row-major in a ceil(sqrt(n))-column grid, with uniform cells sized from the largest panel aspect ratio so nothing clips. A title band is reserved for the whole grid only if at least one panel is titled — an all-untitled montage wastes no vertical space. Tune the geometry with panel_width, title_height, and pad.","title":"The grid"},{"category":"section","location":"man/montage/#Output","page":"Montage","text":"Like every verb, montage writes by default (to montage.svg in the working directory) and also returns the composed SVG string:\n\nmontage(panels)                       # writes ./montage.svg, returns the SVG string\nmontage(panels; output=\"grid.svg\")    # choose the path\nmontage(panels; output=nothing)       # return the string, write nothing\n\nWriting errors if the target exists unless you pass overwrite=true.","title":"Output"},{"category":"section","location":"man/montage/#Comparing-dynamics","page":"Montage","text":"If a panel's content is a vector of frame paths (one per timepoint) instead of a single path, montage becomes a montage of movies: every panel animates through its frames in lockstep. This needs the movie extension — see Movies.","title":"Comparing dynamics"},{"category":"section","location":"man/montage/#Choosing-which-cells-appear","page":"Montage","text":"For the PhysiCell methods, cell_types and include_dead restrict which cells are drawn — with no re-rendering, because PhysiCell tags every cell in its snapshot SVGs with its type and dead flag, so the non-matching groups are simply dropped:\n\nmontage(Simulation, ids; cell_types = [\"tumor_epi\", \"tumor_mes\"])   # tumour only\nstoryboard(Simulation, 1; include_dead = false)                     # live cells only\n\nPhysiCell's own \"N agents\" caption is rewritten to the number actually shown, and the legend narrows to the kept types — a figure whose key advertises types you just filtered out would be misleading.","title":"Choosing which cells appear"},{"category":"section","location":"man/montage/#Colouring-cells-by-data","page":"Montage","text":"color names a cells-table column and repaints each cell along colormap — again without re-rendering, by joining each SVG cell group's id=\"cell…\" to the ID column and rewriting its fill:\n\nmontage(Simulation, ids; color = :pressure)                 # viridis + a colorbar\nstoryboard(Simulation, 1; color = :damage, colormap = :plasma)\n\nThe cell-type legend is replaced by a colorbar, since per-type swatches would describe colours the figure no longer uses. Its range is pooled over every panel (and every frame of a movie) and computed after any cell_types/include_dead filtering, so a colour means the same thing everywhere in the figure — the point of a montage — and excluded cells cannot stretch the scale.\n\nThis path carries a small built-in set of colormaps (:viridis, :plasma, :grays) so it stays dependency-free; tableau re-plots through Makie and has the full set.","title":"Colouring cells by data"},{"category":"section","location":"man/montage/#Editing-panel-contents:-the-transform-seam","page":"Montage","text":"Both of the above are built on a general hook. A Panel carries a transform, a function from SVG text to SVG text applied just before the panel is placed:\n\nmontage([\"a/final.svg\", \"b/final.svg\"];\n        legend = [(\"mine\", \"purple\")],\n        legend_position = :bottom)\n\n# recolour just the first panel, without touching the file on disk\nmontage([Panel(\"a/final.svg\"; transform = svg -> replace(svg, \"fill=\\\"red\\\"\" => \"fill=\\\"purple\\\"\")),\n         Panel(\"b/final.svg\")])\n\nThe transform runs when a panel is placed, so it takes effect only through a verb — constructing a Panel on its own does nothing.\n\nThe default is identity, which is free — it returns the very same string object, so an untransformed composition is byte-identical to one built with no transform support at all.\n\nA movie panel's content is a list of frames, and transform can match it one-for-one:\n\n# one function: every frame edited the same way\nPanel(frames; transform = svg -> replace(svg, \"yellow\" => \"orange\"))\n\n# one function per frame: frame t is edited by transform[t]\nPanel(frames; transform = [tint(t) for t in eachindex(frames)])\n\nThe per-frame form exists because some edits genuinely depend on the timepoint. Dropping a cell type does not — the same function works for every frame — but colouring cells by a data value does, since each frame has to be mapped from its own snapshot's values. That is exactly how color builds its movie transforms.","title":"Editing panel contents: the transform seam"},{"category":"function","location":"man/montage/#Montage.montage","page":"Montage","text":"montage(panels; backend=:svg, ncols=<auto: ceil(sqrt(n))>, panel_width=300,\n        title_height=34, pad=12,\n        legend=nothing, legend_position=:auto, legend_font_size=<title size>,\n        output=<auto: montage.svg | montage.mp4>, overwrite=false, framerate=15)\n\nCompose panels into a uniform titled grid — the verb for comparing like-for-like across many things (e.g. the final state of every simulation in a batch, side by side).\n\nThe panel content decides still image vs. movie, in one call: if every panel's content is a single image (an SVG path), the result is a still image; if any panel's content is a Vector of frame paths (one per timepoint), every panel plays its frames in lockstep and the result is a movie (rendered via record, which needs using Rsvg, Cairo, FFMPEG).\n\nArguments\n\npanels: a Vector{Panel}, or a loose vector of raw contents (each wrapped as an untitled Panel). A panel's content is an SVG path (still) or a Vector of SVG paths (frames of a movie).\n\nKeyword Arguments\n\nbackend::Symbol=:svg: :svg (default, built into the core — lossless vector, static) or :makie (real heatmaps/colorbars; requires using CairoMakie).\npanel_width::Real=300: displayed width in px of each panel.\ntitle_height::Real=34: px reserved above each panel for its title. The band is reserved for the whole grid only if at least one panel is titled.\npad::Real=12: px of padding between and around panels.\nncols::Union{Nothing,Integer}=nothing: columns in the grid; nothing (the default) keeps the near-square ceil(sqrt(n)) layout. Set it to shape the comparison — e.g. a conditions-by-replicates grid via ncols=<number of conditions> with panels listed row-major. Applies to stills and movies alike.\nlegend: what legend to draw — [(\"label\", \"color\"), …] entries (drawn as flat circles and labels, so they stay editable in Illustrator/PowerPoint), or a path/SVG string to nest as-is. nothing (the core default) draws none. With PhysiCellOutput or PCMM loaded the default becomes :auto, which means \"take the cell types from the run's own legend.svg\".\nlegend_position: where it goes — :auto (the free cells trailing the last row if the layout has any, so the figure does not grow, else a full-width band below), :bottom, :top, or an explicit (row, col) / (row, col, span) cell. Independent of legend, so any content can take any placement.\nlegend_font_size::Real: text size for a drawn legend. Defaults to the panel-title size, so the two match. Drawn legends keep this size and wrap to fit the space; a nested SVG legend instead sits at its natural size, shrunk only if it will not fit.\noutput::Union{Nothing,AbstractString}: where to write the result, in the current directory by default — montage.svg for a still image, montage.mp4 for a movie. Errors if the file exists unless overwrite=true. Pass output=nothing to skip writing and return the in-memory result instead.\noverwrite::Bool=false: allow writing over an existing output file.\nframerate::Integer=15: frames per second (movies only).\n\nReturns\n\nThe written path's result: the composed SVG String for a still image, or the output path for a movie. With output=nothing, the in-memory object instead — the SVG String for a still image, or a MontageSpec for a movie (which you can then hand to record for finer control, e.g. scale).\n\nExamples\n\nusing Montage\n\n# Still image — written to ./montage.svg by default, and returned as a string\nsvg = montage([Panel(\"a/final.svg\"; title=\"A\"), Panel(\"b/final.svg\"; title=\"B\")])\nmontage([\"a/final.svg\", \"b/final.svg\"]; output=\"grid.svg\")   # or choose a path\n\n# A legend of your own, banded below rather than tucked into a spare cell\nmontage([\"a/final.svg\", \"b/final.svg\"];\n        legend=[(\"tumor\", \"grey\"), (\"immune\", \"green\")], legend_position=:bottom)\n\n# Montage of movies: each panel is a frame sequence → one call writes ./montage.mp4\nusing Rsvg, Cairo, FFMPEG                                    # movie extension\nmontage([Panel([\"a/f1.svg\", \"a/f2.svg\"]; title=\"A\"),\n         Panel([\"b/f1.svg\", \"b/f2.svg\"]; title=\"B\")]; output=\"compare.mp4\", framerate=15)\n\n\n\n\n\n","title":"Montage.montage"},{"category":"type","location":"man/montage/#Montage.Panel","page":"Montage","text":"Panel(content; title=\"\", transform=identity)\nPanel(content, title)\n\nOne cell of a composition: some content plus an optional title drawn above it.\n\nArguments\n\ncontent: what to draw. For the SVG backend this is a path to an SVG file (AbstractString). Future Makie-backed content (a plotting callback + data) also lands here.\ntitle::AbstractString: text drawn in a band above the content. An empty title (the default) means no title band is reserved for this panel — an all-untitled composition has no wasted vertical space.\ntransform: a function applied to this panel's SVG text before it is placed — SVG text -> SVG text. The seam for editing a panel's contents without re-rendering it: the PhysiCell extension uses it to drop or recolour cells. identity (the default) is free — it returns the very same object, so the output is byte-identical to no transform at all.\nA movie panel's content is a list of frames, and transform can match it one-for-one:\none function — applied to every frame (e.g. \"drop the nk cells\", which does not depend on time);\na list of functions, the same length as the frames — then frame t is edited by transform[t].\nThe per-frame form exists because some edits do depend on the timepoint: colouring cells by a data value needs each frame mapped from that snapshot's own values, so each frame needs its own function.\n\nExamples\n\nPanel(\"output/final.svg\"; title=\"Sim 1\")\nPanel(\"output/final.svg\")                                  # untitled\nPanel(\"output/final.svg\"; transform = s -> replace(s, \"red\" => \"blue\"))\n\n\n\n\n\n","title":"Montage.Panel"},{"category":"section","location":"man/extensions/#Extensions-and-PhysiCell","page":"Extensions & PhysiCell","text":"Montage keeps a light core: the SVG-stitch backend behind montage and storyboard has no heavy dependencies. Everything that needs a big stack lives in a package extension, loaded automatically only when you bring the relevant packages. Requesting a feature without its extension loaded produces a clear error telling you what to using.\n\nExtension Load by Unlocks\nMontageMovieExt using Rsvg, Cairo, FFMPEG rendering montage-of-movies with record\nMontageCairoMakieExt using CairoMakie tableau and its data-driven layout engine\n\nBecause the core owns the verb functions, extensions only add methods to them — so the same montage/tableau you already call simply gains new capabilities when a dependency is present.","title":"Extensions & PhysiCell"},{"category":"section","location":"man/extensions/#PhysiCell-simulations","page":"Extensions & PhysiCell","text":"Montage originated in the PhysiCell ecosystem, and provides convenience methods that build compositions straight from simulation output — e.g. a grid of final states, a snapshot filmstrip, or a cell/substrate tableau (and its movie) for a single simulation.\n\nToday these are delivered through extensions keyed on PhysiCellModelManager.jl (PCMM), which resolve a simulation to its output folder and read its data.\n\nnote: Planned change\nThis PhysiCell integration is slated to move into a dedicated PhysiCellMontage.jl package. That package will also support PhysiCell users who don't use PCMM — driving the verbs from an output-folder path rather than a simulation id — while Montage.jl itself stays PhysiCell-free. Because it relocates the montage(::Type{Simulation}, …) methods off of Montage, it is a breaking change and is being settled before Montage's first registration.","title":"PhysiCell simulations"},{"category":"section","location":"man/movies/#Movies","page":"Movies","text":"Static vs. animated is an orthogonal axis: the composition verbs also produce movies. There is no separate movie= keyword and no _gif/_movie function variants — you build a composition and render it.","title":"Movies"},{"category":"section","location":"man/movies/#Montage-of-movies","page":"Movies","text":"Give montage panels whose content is a vector of frame paths (one per timepoint) and it renders a movie where every panel plays through its frames in lockstep — ideal for comparing dynamics across runs. This uses the movie extension, loaded when you bring Rsvg, Cairo, and FFMPEG:\n\nusing Montage, Rsvg, Cairo, FFMPEG\n\nmontage([Panel([a1, a2, a3]; title=\"A\"),      # a1,a2,a3 are per-timepoint SVG paths\n         Panel([b1, b2, b3]; title=\"B\")];\n        output = \"compare.mp4\", framerate = 15)\n\nUnder the hood the two-step is available too: with output=nothing, an animated montage returns a MontageSpec that record animates.\n\nspec = montage(animated_panels; output=nothing)   # a MontageSpec\nrecord(spec, \"compare.mp4\"; framerate=15)\n\nFrames are aligned by index and truncated to the shortest sequence (time-based alignment is a planned enhancement).\n\nRendering rasterizes each timepoint's composed SVG (via Rsvg/Cairo) and encodes the sequence with FFMPEG, preserving the exact styling of the source SVGs.","title":"Montage of movies"},{"category":"section","location":"man/movies/#Tableau-movies","page":"Movies","text":"A tableau can be animated over time — the whole data-driven scene evolving frame by frame via Makie.record (CairoMakie). This is driven by a data-source method; for PhysiCell simulations it is index=:all (or a range of snapshots) on the simulation method — see Extensions & PhysiCell.","title":"Tableau movies"},{"category":"function","location":"man/movies/#Montage.record","page":"Movies","text":"record(spec::MontageSpec, path=\"montage.mp4\"; framerate=15, scale=1, overwrite=false) -> path\n\nRender a movie of spec to path (extension picks the container, e.g. .mp4/.gif; defaults to montage.mp4 in the current directory), with every panel playing through its frames in lockstep. Each output frame is the montage of that timepoint's frames. Errors if path exists unless overwrite=true.\n\nRequires the movie extension — run using Rsvg, Cairo, FFMPEG — otherwise a helpful error is thrown.\n\nKeyword Arguments\n\nframerate::Integer=15: frames per second.\nscale::Real=1: rasterization scale factor; >1 renders sharper (larger) frames.\noverwrite::Bool=false: allow writing over an existing path.\n\nExamples\n\nusing Montage, Rsvg, Cairo, FFMPEG\nspec = montage([Panel([\"a/f1.svg\",\"a/f2.svg\"]; title=\"A\"),\n                Panel([\"b/f1.svg\",\"b/f2.svg\"]; title=\"B\")])\nrecord(spec, \"compare.mp4\"; framerate=15)\n\n\n\n\n\n","title":"Montage.record"},{"category":"type","location":"man/movies/#Montage.MontageSpec","page":"Movies","text":"MontageSpec\n\nA movie-able composition: a uniform grid of frame-sequence panels (each panel's content is a Vector of per-timepoint frame paths), a common nframes count, and the grid layout parameters. Built by montage when its panels are animated, and consumed by record.\n\nFrames are aligned by index and truncated to the shortest sequence (see nframes).\n\nThe legend_svg/legend_position/legend_font_size fields carry the composition's legend (see _svgGrid) so every rendered frame draws the same one — a legend that would otherwise flicker or vanish mid-movie. The five-argument constructor omits them (no legend).\n\n\n\n\n\n","title":"Montage.MontageSpec"},{"category":"section","location":"man/tableau/#Tableau","page":"Tableau","text":"tableau composes a single state as a focal panel surrounded by satellite panels — the verb for showing how the heterogeneous parts of one state relate spatially (e.g. a focal scatter centered, with heatmaps of related fields arranged around it, all on a shared extent).\n\nUnlike montage and storyboard, tableau is data-driven — it re-plots from data rather than stitching existing SVGs, so it can produce real heatmaps, colorbars, and shared axes. That requires CairoMakie:\n\nusing CairoMakie, Montage\n\nWithout it loaded, calling tableau errors with that hint.","title":"Tableau"},{"category":"section","location":"man/tableau/#The-generic-form","page":"Tableau","text":"The data-agnostic method takes an axis callback for the focal panel and a vector of callbacks for the satellites (each returns the plot its colorbar reads):\n\nusing CairoMakie, Montage\n\nxs = range(-1, 1; length=40)\nZ  = [exp(-(x^2 + y^2)) for x in xs, y in xs]\n\ntableau(ax -> scatter!(ax, randn(50), randn(50); label=\"points\"),\n        [ax -> heatmap!(ax, xs, xs, Z)];\n        satellite_titles = [\"field\"], colorbar_labels = [\"field\"],\n        output = \"scene.png\")\n\nSatellites are auto-ringed around the focal panel; each is paired with a colorbar. The cell-type legend is placed by the legend keyword: :auto (default) drops it in an empty grid cell — off the focal plot entirely — falling back to an in-axis corner when the grid is full; you can also pass a position Symbol (:rt, :lt, …), an explicit grid cell (row, col), or nothing.\n\nSet focal_colorbar_label to give the focal panel a colorbar of its own — for a focal plot encoding a continuous value rather than discrete categories. The focal callback must then return its plot, the same convention the satellites follow, and there are no labelled series for a legend to read, so pass legend=nothing with it.\n\nOutput follows the same rules as the other verbs (writes tableau.png by default; output=nothing returns the Makie Figure; overwrite guards existing files).","title":"The generic form"},{"category":"section","location":"man/tableau/#Vector-output","page":"Tableau","text":"The file extension picks the format — CairoMakie renders .png, .svg and .pdf, so a publication-ready vector figure is just a different output. This applies to every tableau method; the examples below use the PhysiCell one (using PhysiCellModelManager):\n\ntableau(Simulation, 1; output = \"figure.pdf\")     # vector, for a paper\ntableau(Simulation, 1; output = \"figure.svg\")     # vector, to hand-edit\n\nFor one cell layer plus a single substrate (511 cells, a 50×50 voxel grid) the three formats come out very differently:\n\nformat size notes\n.png 154 KB raster; the default, best for exploring\n.pdf 52 KB vector, and the smallest of the three — the natural choice for publication\n.svg 739 KB vector, but uncompressed text, so much the largest\n\nTwo caveats worth knowing before reaching for .svg:\n\nText is converted to outlines. CairoMakie's SVG contains no <text> elements at all — labels become glyph paths — so you cannot retype an axis label in Illustrator. (The stitched verbs, montage and storyboard, do emit real <text>, so their titles and legends stay editable. If editable text is what you need, that is the path that gives it.)\nHeatmaps become one path per voxel. The bulk of that 739 KB is ~2500 <path> elements for a 50×50 substrate grid, not the cell scatter. A finer mesh or more substrates inflates it quickly.\n\nA tableau movie still needs a video container; asking for .svg there fails in Makie's recorder rather than silently producing something odd.","title":"Vector output"},{"category":"section","location":"man/tableau/#Choosing-which-cells-appear,-and-what-their-colour-means","page":"Tableau","text":"For the PhysiCell methods, the focal cell layer is configurable in two independent ways.\n\nWhich cells — cell_types selects by name (validated against the cell types the config defines, so asking for one that is momentarily absent is not an error), and include_dead=false drops cells flagged dead:\n\ntableau(Simulation, 1; cell_types = [\"nk\", \"caf\"])       # just these two\ntableau(Simulation, 1; include_dead = false)             # live cells only\n\nWhat the colour encodes — color names any column of the cells table (see cellLabels(snapshot) for the ~130 available). The column's type picks the visual mode:\n\ncolor mode key\n:cell_type_name (default), or any string/bool column categorical one labelled series per value + a legend\n:pressure, :damage, :total_volume, … continuous a cell_colormap ramp + a colorbar\n\ntableau(Simulation, 1; color = :pressure)                          # ramp + colorbar\ntableau(Simulation, 1; color = :current_phase, color_mode = :categorical)\n\ncolor_mode (:auto, :categorical, :continuous) overrides the choice — needed for numeric-but-discrete columns such as :current_phase, which look wrong on a continuous ramp. Note cell_colormap is the cells' ramp and is deliberately separate from colormap, which belongs to the substrate heatmaps.\n\nColours are chosen to make figures comparable with one another:\n\nCells are drawn in PhysiCell's own colours, read from the run's legend.svg. So a tableau and a montage/storyboard of the same simulation agree, and the types keep the colours you already recognise from PhysiCell's output rather than being reassigned from Makie's palette.\nWhere a colour is not known (a run without legend.svg, or a categorical column that is not a cell type), palette slots are used and pinned to the config's cell-type list rather than to plotting order — so caf keeps the same colour whether you plot every type or filter down to [\"caf\", \"nk\"].\nIn a movie, a continuous color gets a globally fixed colorrange — its min/max across every frame, computed after filtering — so the scale is comparable frame to frame, exactly as the substrate heatmaps already are. This matters more than it sounds: at t = 0 a quantity like pressure is often uniformly zero, which a per-frame scale would render as a meaningless full-range spread.","title":"Choosing which cells appear, and what their colour means"},{"category":"section","location":"man/tableau/#Animating-over-time","page":"Tableau","text":"With an interactive time dimension, a tableau can be animated: the whole scene evolves frame by frame via Makie.record. The generic layout is reused — the plotting callbacks draw Observables that the record loop updates per frame. In practice you drive this through a data-source method (for PhysiCell simulations, index=:all on the simulation method); see Movies and Extensions & PhysiCell.","title":"Animating over time"},{"category":"function","location":"man/tableau/#Montage.tableau","page":"Tableau","text":"tableau(args...; kwargs...)\n\nCompose one state as a focal panel surrounded by satellite panels — the verb for showing how the heterogeneous parts of a single state relate spatially (e.g. a cell layer centered with substrate heatmaps arranged around it, sharing a spatial extent).\n\nUnlike montage and storyboard, tableau is CairoMakie-only and data-driven — it re-plots from data rather than stitching SVGs — so it becomes available only once the CairoMakie extension is loaded (using CairoMakie); without it, calling tableau errors with that hint. Two methods are then provided:\n\ntableau(focal, satellites; …) — the data-agnostic form (MontageCairoMakieExt): focal and satellites are axis callbacks (ax -> …); it arranges them and writes the figure.\ntableau(::Type{Simulation}, sim_id; …) — the PhysiCell convenience form (MontageCairoMakiePCMMExt, also needs PhysiCellModelManager), which builds those callbacks from a simulation's cells + substrates.\n\n\n\n\n\n","title":"Montage.tableau"},{"category":"section","location":"reference/#API-reference","page":"API reference","text":"The exported API. Each entry links to its full docstring in the manual page where it is introduced.\n\n","title":"API reference"},{"category":"section","location":"#Montage.jl","page":"Home","text":"Montage composes visualizations into intentionally-structured composite figures — and movies. It gives you three verbs for the three distinct things a composite figure can say, and treats static vs. animated as an orthogonal choice.\n\nVerb Use it to… Looks like\nmontage compare like-for-like across many things uniform grid of equal panels\nstoryboard show one thing evolving over time ordered filmstrip\ntableau show how heterogeneous parts of one state relate spatially focal panel + satellites","title":"Montage.jl"},{"category":"section","location":"#Design-in-one-breath","page":"Home","text":"A light core. The default backend stitches SVG files into a composite — no heavy dependencies. montage and storyboard work out of the box.\nExtensions add power, not weight. Movies (via record) and tableau (via CairoMakie) live in package extensions, loaded only when you bring their dependencies.\nTyped panels. Compositions are built from Panels, so the intentional structure is explicit rather than implied by tuple positions.","title":"Design in one breath"},{"category":"section","location":"#Installation","page":"Home","text":"pkg> registry add https://github.com/drbergman-lab/BergmanLabRegistry\npkg> add Montage\n\nMontage is registered in the BergmanLabRegistry, alongside PhysiCellModelManager.jl and PhysiCellOutput.jl, so the registry only needs adding once.","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using Montage\n\n# Compare things side by side — writes ./montage.svg, and returns the SVG string\nmontage([Panel(\"a/final.svg\"; title=\"A\"), Panel(\"b/final.svg\"; title=\"B\")])\n\n# One thing over time, as an ordered strip — writes ./storyboard.svg\nstoryboard([Panel(\"t0.svg\"; title=\"t = 0\"),\n            Panel(\"t1.svg\"; title=\"t = 120\"),\n            Panel(\"t2.svg\"; title=\"t = 240\")])\n\nEvery verb writes its output by default and also returns it; pass output=nothing to get the result back without writing, or output=\"path\" to choose where it goes. Movies and tableau need their extensions — see Movies and Tableau.","title":"Quick start"},{"category":"section","location":"#Manual","page":"Home","text":"Pages = [\n    \"man/montage.md\",\n    \"man/storyboard.md\",\n    \"man/tableau.md\",\n    \"man/movies.md\",\n    \"man/extensions.md\",\n    \"reference.md\",\n]\nDepth = 1","title":"Manual"},{"category":"section","location":"#PhysiCell-simulations","page":"Home","text":"Montage grew up in the PhysiCell ecosystem. Convenience methods that build compositions straight from simulation output are provided through an extension today, and are planned to move to a dedicated PhysiCellMontage.jl package — see Extensions & PhysiCell.","title":"PhysiCell simulations"},{"category":"section","location":"man/storyboard/#Storyboard","page":"Storyboard","text":"storyboard stitches an ordered sequence of frames into a single static figure — the verb for showing one subject evolving over time (a filmstrip for a poster or paper). Where montage is an unordered grid, storyboard reads in time order and defaults to a single row.\n\nusing Montage\n\nstoryboard([Panel(\"t0.svg\"; title=\"t = 0\"),\n            Panel(\"t1.svg\"; title=\"t = 120\"),\n            Panel(\"t2.svg\"; title=\"t = 240\"),\n            Panel(\"t3.svg\"; title=\"t = 360\")])\n\nTitles are exposed precisely so you can label each frame with its timestamp. Set ncols to wrap a long strip into a grid while preserving time order:\n\nstoryboard(frames; ncols=4)   # wrap into rows of four\n\nStoryboard is deliberately static — a frame sequence panel is rejected. For a single-subject movie, animate its frames with montage instead (see Movies). Output behaves exactly as for montage (writes storyboard.svg by default; output=nothing returns the string; overwrite guards existing files).","title":"Storyboard"},{"category":"function","location":"man/storyboard/#Montage.storyboard","page":"Storyboard","text":"storyboard(panels; backend=:svg, ncols=length(panels), panel_width=300,\n           title_height=34, pad=12, legend=nothing, legend_position=:auto,\n           legend_font_size=<title size>, output=\"storyboard.svg\", overwrite=false)\n\nStitch an ordered sequence of panels into a single static figure — the verb for showing one subject evolving over time (e.g. a simulation's snapshots left to right).\n\nUnlike montage (an unordered ceil(sqrt(n)) grid), storyboard lays panels out in reading order and defaults to a single row (ncols = length(panels)); set ncols to wrap the strip into that many columns while preserving time order.\n\nArguments\n\npanels: a Vector{Panel}, or a loose vector of raw contents (each wrapped as an untitled Panel). Each panel's content is a single SVG path — a frame sequence is not allowed (storyboard is static; use montage for movies). Titles are where timestamps go.\n\nKeyword Arguments\n\nbackend::Symbol=:svg: :svg (default) or :makie (requires using CairoMakie).\nncols::Integer=length(panels): columns in the strip; the default is a single row.\npanel_width, title_height, pad, legend, legend_position, legend_font_size: as in montage. A single-row strip has no spare grid cell, so legend_position=:auto lands the legend in a full-width band below the frames.\noutput::Union{Nothing,AbstractString}=\"storyboard.svg\": where to write the SVG (cwd by default); errors if it exists unless overwrite=true. output=nothing returns the SVG string without writing.\noverwrite::Bool=false: allow writing over an existing output file.\n\nReturns\n\nThe composed SVG document as a String (written to output unless output=nothing).\n\nExamples\n\nusing Montage\n\n# A four-frame time strip with timestamp titles → writes ./storyboard.svg\nstoryboard([Panel(\"t0.svg\"; title=\"t = 0\"),   Panel(\"t1.svg\"; title=\"t = 120\"),\n            Panel(\"t2.svg\"; title=\"t = 240\"), Panel(\"t3.svg\"; title=\"t = 360\")])\n\n\n\n\n\n","title":"Montage.storyboard"}]
}
